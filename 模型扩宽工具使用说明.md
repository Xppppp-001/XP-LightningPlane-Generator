# XP模型扩宽工具 V1.2.0

适用 Autodesk 3ds Max 2023 中文版。用于已经弯曲、扭转过的规则四边面带状模型，例如刀光片、飘带和闪电片。支持宽度有多列面、保留真实中线的模型，以完整的一排横截面为单位向两侧扩宽。

V1.0.0 只支持每排两个顶点；在“左边—中线—右边”模型上选一侧会移动中线，选两侧会触发共享顶点错误。**请先 Ctrl+Z 撤销旧版错误扩宽，再打开 V1.2.0。** 新版不会推断或恢复已经丢失的历史坐标。

## 打开与使用

1. 将 `dist/XP模型扩宽工具_V1.2.0.mzp` 拖入 3ds Max 视口，打开中文窗口。标题应为 **V1.2.0**。也可在“脚本 → 运行脚本”中运行 `src/XP_RibbonWidener.ms`。重启 Max 后重新运行即可，无须安装其他插件。
2. 只选中一个静态模型。模型应为“可编辑多边形（Editable Poly）”，且修改器堆栈中没有其他修改器。需要保留修改器的源模型时，先自行复制一个独立副本并转换副本。
3. 进入“边”子对象层级，选择需要调整的各排横边。可以用“环形（Ring）”选齐一侧，也可以选两侧全部横边；新版会沿网格拓扑补齐每排另一侧，同一排只处理一次。检查首尾两排是否也被选中。只选部分排时，只扩宽那些排，不会自动补选整条模型的其余排。
4. 输入“宽度倍率”，点击“应用宽度倍率”。
5. 用 **Ctrl+Z** 撤销本次调整；可用 Max 的“重做”恢复该调整。

```text
一排横截面（宽度两列）
左边界 ●────●────● 右边界
           中线

倍率 3 后：
左边界 ●────────────●────────────● 右边界
                  中线不动
```

## 窗口控件

| 控件 | 行为 |
| --- | --- |
| 宽度倍率 | 默认 1.5，范围 0.01～100。相对于点击前的当前宽度；调数值本身不修改模型。 |
| 应用宽度倍率 | 先补齐并校验完整横截面，再一次写入顶点；每次点击生成一项“模型截面宽度调整”撤销记录。倍率 1 不产生修改。 |
| 设置起点 | 先只选端部一排横边，再点击记录该完整截面；不修改模型、UV 或选边。重新加载工具、改选其他对象或改变拓扑后必须重新设置。 |
| 起点／终点倍率 | 默认均为 1，范围 0.01～100；相对于各排点击前的宽度，按中线的世界累计弧长线性过渡。 |
| 交换首尾 | 交换两个倍率输入，不改变起点标记，也不修改模型。 |
| 应用首尾渐变 | 设置起点后，选齐整条模型的横截面（包括首尾），再应用；单／双侧均可。与上方统一倍率独立，每次只执行点击的模式。 |
| 底部状态 | 显示已处理的截面排数、自动补齐的边数、倍率或错误原因。补齐只用于计算，不改变原来的红色边选择。 |

`1.2` 表示宽 20%，`1.5` 表示宽 50%，`2` 表示两倍宽，`0.5` 表示一半宽。**连续两次 1.5 会变成最初的 2.25 倍；输入 1 只保持当前宽度，不是恢复最初宽度。**

## 制作前宽后窄或前窄后宽

1. 在边层级只选择作为起点的端部横边，点击“设置起点”。
2. 用环形选择选齐整条条带的横向截面，检查首尾是否选齐；可以只选一侧或选两侧。
3. 前宽后窄可输入起点 `2`、终点 `0.5`；相反效果输入 `0.5`、`2`。点击“应用首尾渐变”，Ctrl+Z 撤销一次即可恢复。

倍率按每排原宽度计算，保留原来不等宽的基础关系。中线位置、弯曲走势和截面扭转方向保持；宽度及外轮廓会变化。首尾均为 1 不产生修改，相同首尾倍率等同于整条统一扩宽。标记只保存在当前窗口，不写入场景用户属性。

## 保持的内容与限制

- 每排有奇数个顶点时，以拓扑顺序中间顶点为固定中心；截图中的两列模型将保持全部中线顶点。每排有偶数个顶点时，以左右最外侧端点的中点为中心。所有顶点围绕同一中心使用同一倍率，保持截面方向。
- 拓扑、所有已启用映射通道的 UVW 坐标、数量、面索引及支持状态、材质、对象名称、变换和轴心保持；包括接缝、0～1 外坐标、隐藏顶点色通道。不删除、焊接或重建中线，不重展、不归一化 UV；贴图会随几何宽度拉伸。
- 弯曲模型的两侧边界弧长可能随扩宽改变；扩大过多也可能让带的不同部分相交。工具不会重布线或改变中心走势来消除自相交。
- 必须选真正的横边。工具沿四边面连接寻找完整截面，不按世界轴或顶点编号猜方向；它不能在任意模型中区分横向和纵向。明显弯折的整排会被拒绝，但直线纵边仍可能被识别为截面。
- **“选两侧所有横边”可以使用；Ctrl+A 全选模型的纵横所有边不可使用。** 后者会产生交叉截面并被拒绝。宽度可以有一列或多列，每排应保持直线、连续四边面连接。
- 无选择、多选、错误类型、空选边、交叉截面、分叉、闭环、非四边面、非流形连接、零长度／折返、非法倍率、修改器和共享实例均给出中文提示。工具不会自动塌陷修改器或把实例变成独立对象。
- 所有坐标先计算再批量写入，关闭软选择影响与自动关键帧创建；只用于静态建模，不用于调整已有顶点动画。
- 这是直接几何编辑。若之后再用“XP闪电面片生成器”调整同一对象的参数，生成器重建会覆盖这次手工扩宽。

## 验证状态

2026-09-18 已通过指定 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe` 启动的隔离空场景测试，实际版本为 Max 2023.2.2。统一倍率 **3598** 项、渐变与全通道 UV **4161** 项断言通过，共 **7759** 项。证据为 `artifacts/ribbon-gui-status.txt`、`artifacts/ribbon-widener-smoke.txt`、`artifacts/ribbon-gradient-smoke.txt`。MZP 加载、中文控件事件、起点设置、交换、应用、重复加载及撤销／重做均已运行验证；全部启用通道 `-2/-1/0/1/3/7` 的支持状态、数量、UVW 与逐角面索引精确相同，并覆盖 Preserve UVs 开／关。

Windows 视口物理拖放、用户原模型／原贴图的视觉效果，以及异常写入恢复的故障注入仍为**待实机验证**。UV 数据保持不等于消除紧弯重叠；本轮未修改 R-045 的几何约束。

开发者运行 `tools/test-ribbon-widener.ps1`，固定使用上述 exe 路径，隐藏启动独立实例，顺序执行统一扩宽与渐变专项测试并仅退出带令牌的自建实例；已有 Max 运行时拒绝启动第二个实例。测试不打开、重置或保存场景。渐变夹具覆盖直条／弯曲扭转、宽度 1／2／3／4 列、两端作为起点、左／右／双侧、非均匀世界弧长、不等原宽、多通道接缝及未引用／0～1 外 UVW、输入拒绝与 1→1 不占 Undo。静态包和编码校验见 `artifacts/ribbon-widener-static.txt`。

算法与打包 API 参考：[Autodesk 顶点读取／写入](https://help.autodesk.com/cloudhelp/2023/ENU/MAXScript-Help/files/3ds-Max-Objects-and-Interfaces/Editable-Meshes-Splines-Patches/Editable_Poly/Editable_Poly-Methods/GUID-07318A19-8091-4B3E-BB46-463942B0F39B.html)、[Autodesk MZP 包](https://help.autodesk.com/cloudhelp/2023/ENU/MAXScript-Help/files/MAXScript-Tools-and-Interaction/File-Access/Zip-file-Packaging-and-Drag-And/GUID-A085EFDD-A397-41D6-9391-421344294422.html)。
