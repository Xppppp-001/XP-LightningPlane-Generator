# 项目开发规则

## 需求依据

- 开始任何规划、编码、重构、打包或修复工作前，必须完整读取根目录的 `REQUIREMENTS.md`。
- `REQUIREMENTS.md` 是当前版本唯一的需求与验收依据。
- 新需求必须先写入该文件的对应章节和“需求变更记录”，再开始实现。
- 不得自行删除、弱化或改变已确认需求。

## 每轮开发后的强制检查

- 完成修改后，必须再次完整读取 `REQUIREMENTS.md`。
- 对照 `R-001` 至 `R-008` 检查功能遗漏和回归。
- 运行当前环境可以执行的验证；需要 3ds Max 2023 实机验证的项目必须明确标为“待实机验证”。
- 只有具有验证证据的验收项才能标记完成。
- 在 `REQUIREMENTS.md` 的“开发验证记录”中追加本轮结果。
- 最终汇报必须列出本轮完成内容、验证证据、遗漏项和下一步。

## 兼容性底线

- 所有实现以 Autodesk 3ds Max 2023 中文版为目标。
- 用户可见界面和提示使用简体中文。
- V1 不得引入未经确认的第三方运行时依赖。
- 最终包必须支持以 `.mzp` 形式拖入 3ds Max 使用。
