# XP闪电面片生成器：V1.4.0 需求与验收清单

## 1. 文档信息

- 文档状态：R-001～R-015 与 R-019～R-034 已完成既有 Max 2023 核心自动实机回归；R-040 已完成静态实现，并取代 R-011／R-025／R-026／R-031／R-033／R-035～R-039 中按曲率、噪波、Twist、途经点或非共面程度重分配／隐藏增加长度面与横向列的旧规则，生成主链改为“规则直线 Plane → 用户指定总长度面数 → 一次 FFD 式连续变形”；R-016／R-017 轴向修改功能已按用户要求撤销
- 最后更新：2026-08-14
- 目标软件：Autodesk 3ds Max 2023 中文版
- 交付形式：可直接拖入 3ds Max 视口运行或安装的 `.mzp` 包
- 实现原则：不依赖额外 Python 库或第三方 3ds Max 插件
- 本文档是 V1 开发、验收和遗漏检查的唯一需求依据。

## 2. V1 功能需求

### R-001 自定义起点与多个终点

- 用户可以在场景中指定一个闪电起点。
- 用户可以指定一个或多个闪电终点。
- 插件从同一个起点分别生成通往所有终点的闪电分支。
- 用户可以重新指定起点，添加、移除或清空终点，并重新生成结果。
- 每个有效终点必须生成一个独立的新闪电模型对象；不同终点的分支不得附加或合并到同一个 Editable Poly 对象中。
- 多终点强制重新生成采用整组原子新建：只有全部新分支都成功生成后才提交新组；任一分支失败时不创建新组。普通参数更新按 R-019 只原位更新所选单个分支。

验收标准：

- [x] 一个起点和一个终点可以正常生成一条闪电。
- [x] 一个起点和多个终点可以正常生成多条分支。
- [x] 修改起点或终点后可以正确更新或重新生成模型。
- [x] 删除或清空终点不会遗留无效引用或错误模型。
- [x] 多终点时生成对象数量与有效终点数量一致，每个对象只包含通往自身终点的一条分支。
- [x] 多终点强制新建失败时不产生新组；选中单个分支更新失败时该节点及所有其他分支均保持不变。

### R-002 写实闪电路径与平面模型

- 闪电路径具有随机弯折和噪波扭曲，可形成自然、不规则的闪电轮廓。
- 用户可以调节闪电宽度。
- 平面模式生成无厚度的连续带状面片。
- 插件提供中文参数“平面面数”，用于控制每条分支沿长度方向的四边面数量。
- “平面面数”为 `1` 时，每条分支生成一个直接连接起点与终点的四边形。由于没有中间顶点，该模式不能表现路径中段弯曲。
- “平面面数”大于 `1` 时，每条分支生成指定数量的连续四边面，并沿噪波路径弯曲。
- 多终点时，“平面面数”分别作用于每个独立分支对象；每个对象恰好包含指定数量的四边面，所有分支对象的总四边面数等于每条分支面数乘以终点数量。
- 生成结果不得包含退化面、意外重叠面、断裂面、错误的贴图面索引或需要用户手动焊接、修复的拓扑问题。
- 带状面片本身的正常开放边界不视为破面。

验收标准：

- [x] 平面面数设为 `1` 时，每条分支恰好生成一个有效四边形。
- [x] 平面面数设为 `N` 时，每条分支恰好生成 `N` 个连续有效四边面。
- [x] 多终点情况下，总面数符合“面数 × 终点数量”。
- [x] 在最低、中等和较高面数下均不存在退化面、断裂或错误索引。
- [x] 调整宽度后，路径端点和拓扑仍然正确。

### R-003 自动生成和匹配 UV

- 创建或更新模型时，插件自动创建 UV，不要求用户手动展开。
- 使用贴图通道 1。
- 每条分支的 U 方向沿闪电长度展开：起点位于左侧 `U=0`，终点位于右侧 `U=1`。
- V 方向横跨面片宽度，从一侧 `V=0` 到另一侧 `V=1`。
- 每个独立分支对象占满自身的一套 `0–1` UV，以便所有分支复用同一张闪电贴图。
- 改变平面面数、路径参数或终点后，UV 必须随新模型自动重建并保持方向一致。

验收标准：

- [x] 起点端 UV 位于左侧，终点端 UV 位于右侧。
- [x] 每条分支沿长度方向的 U 值连续且不反向。
- [x] V 值正确覆盖面片宽度。
- [x] 几何面与贴图面数量和索引完全匹配。
- [x] 修改面数并重新生成后不需要手动修复 UV。

### R-004 扭曲、噪波和端点锁定

- 插件提供可输入或微调的中文参数，包括：
  - 路径噪波强度（V1.0 界面中的“扭曲强度”在 V1.1 更名，避免与 R-008 的真正 Twist 混淆）
  - 噪波大小
  - 噪波 Tiling／频率
  - 随机种子
  - 平面面数
  - 闪电宽度
- 改变路径噪波参数后，可以更新或重新生成闪电形状。
- 提供独立的“锁定起点”和“锁定终点”选项。
- 锁定起点时，噪波对起点的位移必须精确为零。
- 锁定终点时，噪波对每个终点的位移必须精确为零。
- 锁定端点后，噪波应在端点附近平滑衰减，避免端部出现突然折断。
- 未锁定相应端点时，允许噪波影响该端点及其附近路径。
- 相同输入对象、参数和随机种子应能重复得到相同结果。

验收标准：

- [x] 路径噪波强度和噪波大小能明显改变路径偏移幅度。
- [x] 噪波 Tiling／频率能改变路径弯折密度。
- [x] 相同随机种子可以复现相同模型。
- [x] 锁定起点后，生成路径的起点位置与指定起点完全一致。
- [x] 锁定终点后，每条路径的终点位置与对应指定终点完全一致。
- [x] 锁定端点附近不存在明显的突跳或折断。

### R-005 轴心位置

- 生成对象的轴心始终位于公共起点的中心。
- 一个起点连接多个终点时，每个独立分支对象都使用同一个公共起点作为自身轴心位置。
- 改变参数、面数或重新生成模型后，轴心不得漂移。

验收标准：

- [x] 单终点生成后，对象轴心世界坐标与起点中心一致。
- [x] 多终点生成后，对象轴心世界坐标与公共起点中心一致。
- [x] 多次更新或重新生成后，轴心位置保持一致。

### R-006 中文界面、安装与兼容性

- 插件界面、按钮、参数名称、状态提示和错误信息使用简体中文。
- 插件兼容 Autodesk 3ds Max 2023 中文版。
- 最终交付为 `.mzp` 安装包，可直接拖入 3ds Max 视口运行或完成安装。
- 用户不需要另外安装 Python 包、渲染器或第三方插件即可使用 V1 的几何和 UV 功能。
- 对未指定起点、没有终点、起终点重合、无效对象引用和非法参数等情况给出中文提示，不应导致 3ds Max 崩溃。

验收标准：

- [ ] `.mzp` 可以在干净的 3ds Max 2023 环境中通过拖放启动或安装。
- [x] 所有用户可见文本均为简体中文。
- [x] 无第三方依赖时仍能生成模型和 UV。
- [x] 常见无效输入均有明确中文提示，且不会留下半成品模型。

### R-007 参数实时更新

- 在场景中选中一个具有有效起终点引用的插件面片时，用户调整生成参数后，插件自动原位更新该面片，不需要再次点击“生成／更新”按钮。
- 实时更新覆盖已有参数：平面面数、闪电宽度、路径噪波强度、噪波大小、噪波 Tiling、随机种子、锁定起点和锁定终点。
- 实时更新同时覆盖 R-008 的轴向 Twist 参数和 R-009 的两端收尖选项。
- 拾取或更换起点、添加或移除终点只改变下一次强制重新生成使用的控制点列表；已生成面片继续保存并使用自身的起终点引用。
- 第一次凑齐有效起点和终点后不会仅因参数变化自动创建模型；必须点击“强制重新生成”创建新对象。已有面片的正常参数调整不依赖按钮，而是按 R-019 原位修改当前选中面片。
- 实时重建使用单节点原子提交：失败时恢复所选节点的原几何和用户属性，其他分支始终不参与更新，不留下临时或半成品对象。
- 实时更新过程中不得重复进入生成函数、连续弹出错误窗口或导致 3ds Max 无响应；状态信息使用中文显示在插件窗口中。

验收标准：

- [x] 已有有效模型时，修改每个数值参数都会自动更新几何和 UV。
- [x] 修改平面面数时，面数、顶点数和 UV 会实时按新数值重建。
- [x] 切换起点／终点锁定选项时，端点位置会自动更新，不需要手动生成。
- [x] 拾取或改变控制点不会自动新建或覆盖模型；点击强制重新生成才按当前控制点创建新对象。
- [x] 连续快速调整参数不会产生重复模型、半成品模型或递归更新错误。
- [x] 实时更新失败时保留上一个有效模型，并在界面显示中文状态。

### R-008 整段轴向扭转（Twist）

- 在现有分形噪波之外，提供独立的“整段扭曲”功能。这里的扭曲必须是沿闪电中心线路径的切线轴旋转带状面片截面（真正的 `Twist`），不得用另一层中心线位移或 Noise 代替。
- 整段扭曲只改变面片宽度方向／截面朝向，不改变由细噪波生成的中心线位置；关闭或调整整段扭曲不得暗中增加中心线随机偏移。
- 插件提供以下中文参数：
  - 启用整段轴向扭转（Twist）
  - 轴向扭转分段
  - 轴向扭转角度
- “轴向扭转分段”为大于或等于 `1` 的整数，其等距划分和逐段累计含义以 R-013 为准；它不替代“平面面数”，也不得自行改变用户指定的四边面数量。
- “轴向扭转角度”为每个有效扭转分段的旋转增量；正负值代表相反旋转方向，数值为 `0` 或未启用时不施加轴向 Twist。
- 整段扭曲本身不引入随机噪波；随机种子只继续控制原有细噪波路径。相同输入应得到相同 Twist 结果。
- 整段扭曲与细噪波彼此独立：细噪波决定中心线，Twist 决定带状面片围绕中心线的截面旋转。
- 整段扭曲同时受“锁定起点”和“锁定终点”影响：锁定端点处的截面旋转角精确为零，并在端点附近平滑衰减；未锁定时允许 Twist 影响对应端点的面片朝向。
- 当平面面数为 `1` 且两端均锁定时，因为只有起终点两组截面且两端旋转均为零，整段扭曲不会形成可见效果。
- 启用或调整整段扭曲后，通过 R-007 实时更新模型、拓扑和 UV。

验收标准：

- [x] 启用整段扭曲后，面片宽度方向会沿中心线切线轴发生可见旋转，而中心线与仅有细噪波时保持一致。
- [x] 调整轴向扭转分段会改变等距区间数量和总累计旋转量，但不会改变平面面数。
- [x] 调整轴向扭转角度会改变截面旋转角度，正负值方向相反，设为 `0` 时等同于关闭。
- [x] 锁定起点或终点时，整段扭曲在相应端点的截面旋转角精确为零且平滑衰减。
- [x] 未锁定端点时，整段扭曲允许影响相应端点的面片朝向。
- [x] 多终点、低／中／高面数下仍保持有效四边面拓扑和正确 UV。
- [x] 相同参数可复现相同的整段扭曲结果；改变随机种子只改变细噪波中心线，不把 Twist 退化为随机 Noise。

### R-009 面片两端平滑收尖

- 插件提供中文选项“面片两端平滑收尖”，默认关闭。
- 开启后，每条闪电分支在起点和对应终点附近沿长度方向平滑缩小面片宽度，中段保持用户设置的“闪电宽度”，形成两端尖、中间宽的带状轮廓。
- 为继续满足 R-002 的有效四边面和无需修破面要求，端点宽度缩小到正常宽度的 `1%`，不把左右顶点完全合并为会产生退化面的同一点。
- 收尖只改变左右顶点相对中心线的宽度，不得改变细噪波中心线、轴心、平面面数、面片连续性或 R-008 的轴向 Twist 方向。
- UV 仍使用贴图通道 1，U 方向继续从起点 `0` 到终点 `1`，V 方向仍为 `0–1`；收尖不得破坏贴图面数量或索引。
- 多终点时，每条分支分别在公共起点和自身终点收尖。
- 当平面面数为 `1` 时没有中间截面可表现渐变，两端均保持 `1%` 宽度，生成一张窄但不退化的有效四边面。
- 切换该选项后通过 R-007 实时更新模型和 UV。

验收标准：

- [x] 默认关闭时几何与加入该功能前保持一致。
- [x] 开启后起点和终点宽度为中段宽度的 `1%`，端点附近宽度平滑过渡，中段保持用户指定宽度。
- [x] 收尖开关不改变中心线、轴心、平面面数和 UV 布局。
- [x] 与轴向 Twist 同时启用时仍保持有效连续四边面，并同时表现截面旋转与两端收尖。
- [x] 多终点及每支 `1`／`8`／`64` 面时拓扑和 UV 均有效，无需手动修复。
- [x] 收尖选项支持实时更新，失败时继续保留上一个有效模型。

### R-010 Twist 面片抗折叠与安全优化

- 轴向 Twist 生成的每个四边面除不得退化外，还必须通过局部折叠检查，避免出现截图中的严重非共面三角化条纹、翻面和宽面片折回。
- 插件必须限制相邻截面宽度方向的单步旋转，并检查一个四边面内部两种三角区域的法线一致性；安全阈值应写入代码常量并由测试覆盖。
- 安全检查不得移动细噪波中心线、修改用户指定的平面面数或把 Twist 退化为 Noise。
- 当当前面数、宽度和 Twist 参数组合无法安全表达所需旋转时，插件应拒绝本次整组更新，保留旧分支对象，并以简体中文提示用户增加平面面数或降低轴向扭转角度、分段、宽度或噪波强度。
- 未启用 Twist 或处于安全参数范围时，不得无故改变既有几何、UV、轴心和收尖结果。

验收标准：

- [x] 已接受生成的所有四边面均通过非退化、法线一致性和相邻截面旋转阈值检查。
- [x] 已知的低面数／高 Twist 折叠组合会被原子拒绝，且不会覆盖或删除旧的有效分支对象。
- [x] 抗折叠检查不改变中心线、用户指定面数、UV 方向或公共起点轴心。
- [x] 多终点独立对象模式下，任一分支不安全会拒绝整组替换，不会只更新部分终点。

### R-011 曲率与 Twist 自适应布线

- 插件不得只依赖生成后的拒绝检查来处理局部挤线；应在构建几何前根据中心线曲率和轴向 Twist 变化动态分配纵向线环。
- “平面面数”继续表示每条分支最终的精确四边面数量 `N`；自适应布线只能重新分配这 `N` 个面在路径上的位置，不得暗中增加或减少面数。
- 自适应采样必须在转弯较急或 Twist 变化较快的区段分配更密的截面，在平直、低变化区段分配较疏的截面，并保持起点、终点、随机确定性和中心线路径定义。
- 当局部曲率半径相对于闪电宽度过小时，应沿中心线平滑降低该区域的实际截面宽度，避免内侧边界折返、多个横向边聚集到同一点或形成扇形夹面；离开急弯后应平滑恢复用户宽度。
- 曲率安全宽度只允许缩放左右顶点到中心线的距离，不得移动中心线、改变 Twist 方向、UV 的 `0–1` 方向、公共起点轴心或独立分支对象规则。
- 自适应处理后仍无法满足 R-010 安全阈值时，继续使用整组原子拒绝和中文提示作为最后保护。

验收标准：

- [x] 自适应前后每条分支均精确保持用户指定的 `N` 个四边面和 `2 × (N+1)` 个顶点。
- [x] 急弯／高 Twist 区段的采样密度高于同一路径的平直区段，且同一输入可确定复现。
- [x] 曲率过紧时局部宽度会平滑收窄，内侧边界不折返，离开急弯后恢复正常宽度。
- [x] 自适应布线不改变锁定起终点、中心线定义、轴心和从左到右的 UV。
- [x] 用户截图参数（100 面、宽度 3、噪波强度 15、噪波大小 0.5、Tiling 1、种子 1、Twist 分段 3、角度 75、两端锁定）可生成通过 R-010 检查的模型，而不是只保留旧模型。

### R-012 FFD 式逻辑空间 Twist

- R-008 的轴向 Twist 实现顺序调整为类似 FFD 的逻辑：先把带状面片视为沿 `0–1` 长度方向拉直的参考带，在该直线逻辑空间中施加连续累计旋转，再将旋转后的截面映射到细噪波中心线。
- 从直线参考带映射回弯曲中心线时，必须使用最小旋转／平行运输框架；不得逐点重新猜测截面方向，也不得让路径曲率和 Twist 角度相互叠加放大。
- “轴向扭转角度”在 R-013 中进一步明确为“每个有效扭转分段的旋转增量”，不再解释为整条面片仅累计一次该角度；不得使用中段达到满角后整段反向旋回的正弦振幅。
- “轴向扭转分段”的最终等距划分与逐段累计语义以 R-013 为准；仍不得制造随机摆动或明显的分段折角。
- 关闭 Twist 或角度为 `0` 时，输出必须等于仅由平行运输框架构建的未扭转参考带；正负角度必须产生相反方向的连续累计旋转。
- FFD 式 Twist 继续遵守 R-010／R-011：保持精确面数、动态布线、曲率安全宽度、独立对象、整组原子替换和 `0–1` UV。

验收标准：

- [x] 直线路径上，截面角度沿长度连续累计，75° 输入不会在中段产生 75° 峰值后整段反向旋回。
- [x] 弯曲路径上的未扭转框架由最小旋转平行运输得到；相邻截面不会因参考轴切换而翻转。
- [x] 对同一逻辑长度位置，直线与弯曲路径应用相同 Twist 角；路径曲率不会放大用户角度。
- [x] 两端锁定时起终点 Twist 角精确为零，只在端点影响区平滑进入／退出；中段仍保持 FFD 式累计趋势。
- [x] 用户截图参数及正负角度、0°、单／多终点、低／中／高面数均通过 Max 2023 实机几何、UV 和实时更新回归。

### R-013 等距分段逐段累计 Twist

- “轴向扭转分段”数值记为 `K`。起点到终点的逻辑长度必须按参数 `t` 等距划分为 `K+1` 份，分界位置为 `i/(K+1)`；例如 `K=2` 时必须得到三个等长区间。
- 前 `K` 个等长区间是有效扭转区间；每个区间都把截面角度从上一段累计值连续旋转到“上一段累计值 + 轴向扭转角度”。因此“轴向扭转角度”是每段增量，未锁定终点时总累计旋转量为 `K × 轴向扭转角度`。
- 每个有效区间内部使用恒定角速度的线性旋转，区间边界的角度连续；不得使用会在区间中部放大瞬时旋转速度的 easing，不得出现随机摆动或把 `K` 仅作为几乎不可见的插值密度参数。
- 最后一个等长区间是终点处理区：未锁定终点时保持前 `K` 段的累计角；锁定终点时按旋转方向的最短等价角平滑回正，使终点截面与未扭转参考截面精确一致，避免为回正额外倒转多个整圈。
- 锁定起点时起点角精确为 `0`；未锁定起点时允许 Twist 影响起点朝向。端点锁定只改变端点处理，不移动细噪波中心线。
- 自适应布线与 Twist 单步安全检查在有效扭转区间内必须检查真实累计角差，不能把每段 `360°/720°` 旋转误当成零；仅在锁定终点最终数值回到 `0°` 的等价表示处使用最短等价角差，避免伪跳变。
- 当精确面数足以采样角度、但面片宽度仍使局部四边面过度非共面时，允许在保持中心线和面数不变的前提下动态平滑收窄该局部截面；离开高 Twist 风险区后恢复用户宽度，最终仍需通过 R-010 安全检查。
- UI 提示必须明确：“分段 `K` 会把长度分成 `K+1` 等份，前 `K` 份各累计一次所设角度”；实时调整分段或角度后自动重建。

验收标准：

- [x] `K=2` 时分界点精确位于长度的 `1/3` 和 `2/3`，前两段分别累计一次输入角度，第三段负责保持或锁定回正。
- [x] 对任意 `K`，未锁定终点的最终累计角等于 `K × 输入角度`，正负输入方向相反，`0°` 等同关闭。
- [x] 相邻等距区间的角度连续；跨越 `±180°/360°` 等价角时几何朝向与安全检查均无伪跳变。
- [x] 锁定终点时最后一段按最短等价角平滑回正，终点角精确为 `0`，不会额外反转多个整圈。
- [x] 用户截图参数（50 面、Twist 10 段、每段 120°、两端锁定、多终点）产生清晰的逐段轴向旋转，并保持独立对象、有效四边面、动态布线和正确 UV。

### R-014 分支对象稳定命名

- 新生成的闪电面片对象统一使用英文名称序列 `LightningPlane001`、`LightningPlane002`、`LightningPlane003`……，不得再按终点名称拼接对象名。
- 每个终点仍对应一个独立面片对象；首次为新终点生成时分配当前场景中可用的下一个 `LightningPlane` 序号。
- 因参数调整或实时更新而改变几何时，必须按 R-019 在当前选中节点原位提交并保留其名称，不得创建替代节点、改变名称或递增序号；“强制重新生成”按 R-015 新建一组并分配新名称。
- 用户手动修改了既有分支对象名称时，同一终点后续重建也应保留该自定义名称。
- 删除或新增面板终点不修改任何已有分支名称；下一次强制重新生成时，新对象获得新的可用 `LightningPlane` 序号。
- 单面片原位更新与强制新建均须满足原子规则：失败时保留所有旧对象及其原名称，不留下临时对象或临时名称。

验收标准：

- [x] 空场景首次生成三个终点时，三个独立对象依次命名为 `LightningPlane001`、`LightningPlane002`、`LightningPlane003`。
- [x] 调整任意生成参数并原位更新所选面片后，该节点及所有其他对象名称与更新前完全一致。
- [x] 用户手动改名后再次原位更新，所选节点保持用户自定义名称。
- [x] 移除或新增面板终点时全部已有分支名称不变；强制重新生成只给新对象分配新的可用序号。
- [x] 单面片更新或整组强制新建失败时，全部旧分支及其原名称保持不变，且场景中不存在临时命名残留。

### R-015 强制重新生成保留旧面片

- 参数数值、锁定选项等普通实时变化按 R-019 只更新场景中当前选中的单个面片；不再默认替换当前最新活动组。
- 用户点击“强制重新生成”时，不得替换、覆盖、删除或改名当前已有面片；必须根据当前起点、终点和参数另行创建一套全新的独立面片对象。
- 强制新建成功后，旧组作为历史结果保留在场景中，新组成为当前活动组；后续实时参数更新仍由场景选择决定，可单独编辑任意有效的新组或历史面片。
- 每次强制新建的对象继续按 R-014 获得新的 `LightningPlane###` 名称；不得继承旧组名称。
- 强制新建采用整组原子提交：任一终点分支失败时不创建新组、不改变当前活动组，也不修改任何历史对象。
- 清除起点、清空终点或移除最后终点只修改下一次强制生成使用的控制点列表，不得删除、重建或修改任何已有面片。

验收标准：

- [x] 单击一次“强制重新生成”后，旧组全部对象仍有效且名称、几何、UV 不变，同时新增一套对象数量与终点数一致的新组。
- [x] 连续单击强制重新生成会逐组累加对象，每组均使用新的 `LightningPlane###` 序号。
- [x] 强制新建后修改普通参数，只原位更新场景中当前选中的单个面片；未选中的新组与历史面片均保持不变。
- [x] 强制新建失败时场景对象、活动组和历史组均保持原状，且无临时节点残留。
- [x] 清除控制点不删除任何已有面片，所有新组与历史组保持有效。

### R-016／R-017 轴向修改功能撤销

- 用户已撤销此前的一键 Unity／Y-Up 轴向适配需求；插件面板不得再提供“设置 Y 轴朝上”“烘焙 Unity 轴向”或同类修改对象轴向的按钮。
- 新生成面片和普通参数原位更新不得为对象安装 `Rotation List`、冻结旋转层或其他仅用于轴向适配的复合动画控制器，避免 FBX 导出出现 `Unsupported controller` 警告。
- 生成器只继续保证 R-005 的轴心位置位于公共起点，不再修改轴心方向，也不再保证 Unity Y-Up 或旋转栏归零。
- 为避免打开插件时静默改变已有场景，旧版本已经执行过轴向适配的历史节点不在脚本加载时自动迁移；用户可通过强制重新生成得到不含该轴向功能的新面片。

验收标准：

- [x] 面板中不存在修改 Unity／Y-Up 轴向的按钮或操作入口。
- [x] 新生成的面片使用标准变换控制器，不包含插件创建的 `Rotation List` 或 Y-Up 冻结层。
- [x] 普通参数原位更新不会新增或继承插件轴向适配标记与控制器。
- [ ] 强制重新生成的新面片可供 FBX 静态导出，不再因本插件的轴向功能触发 `Unsupported controller`。

### R-018 独立中文使用说明与同步维护

- 项目根目录必须提供独立的简体中文使用说明 `插件使用说明.md`，面向插件使用者说明安装、面板控件、推荐工作流程、输出规则、UV、Twist、实时更新、常见提示和已知限制；不得再描述已撤销的 Unity／Y-Up 轴向功能。
- 面板中每个用户可操作控件都必须在说明中有对应条目；参数名称、默认值、范围和行为必须与当前 `src/XP_LightningGenerator.ms` 一致，不得沿用已经废弃的功能解释。
- 说明必须明确区分路径噪波与真正的轴向 Twist，说明 Twist 分段 `K` 的 `K+1` 等分及逐段累计规则，并写明高风险参数被拒绝时旧模型会保留。
- 说明必须明确多终点独立对象、`LightningPlane###` 稳定命名、强制重新生成保留旧组，以及普通参数只原位更新当前选中面片。
- 以后新增、删除、重命名或改变任何用户可见功能、控件、默认值、范围、工作流或限制时，必须在同一轮开发中同步更新 `插件使用说明.md`，并在开发后核对文档与面板源码。
- 用户确认某一版本可作为稳定状态并要求备份时，必须在 `backups` 下建立独立、不可混淆的回退目录；备份至少包含完整源码、对应 MZP、当前中文使用说明、需求文件和 SHA256 清单，并明确备份基线与仍待人工验证的范围。

验收标准：

- [x] 根目录存在可独立阅读的 `插件使用说明.md`，无需阅读源码或需求文档即可完成安装、生成与调整。
- [x] 说明覆盖当前面板全部可操作控件，控件文字、默认值和数值范围与源码一致。
- [x] 说明准确描述实时更新、原子失败保护、多终点独立对象、稳定命名、强制新建历史组、UV 和轴心规则。
- [x] 说明准确描述路径噪波、FFD 式轴向 Twist、`K+1` 等距分段、端点锁定、平滑收尖和动态安全布线。
- [x] 项目开发流程明确要求后续功能或面板更新时同步维护该说明文件。
- [x] 用户确认的 R-031 稳定版本已建立独立备份，包含完整源码、对应 MZP、当前说明、需求与 SHA256 清单。

### R-019 以场景选中面片为参数编辑目标

- 参数实时更新不再默认替换“最新活动组”或整组分支；用户在场景中选中的单个插件面片对象才是当前参数编辑目标。
- 调整平面面数、宽度、噪波、Twist、锁定或收尖等参数时，只更新当前选中的面片对象；其他活动面片、历史面片和同一批次的其他终点分支都不得改变。
- 参数更新必须在原节点上提交新的 Editable Poly 基础几何，保持对象节点身份、名称、父子层级、Transform／轴心、材质及当前选择不变；不得留下新节点，不得递增或重分配 `LightningPlane###` 名称。
- 只有用户点击“强制重新生成”时才按照当前起点、终点和面板参数创建新的 `LightningPlane###` 对象；普通参数调整不得在未选中插件面片时自动创建模型。
- 每个生成面片必须保存自己的全部生成参数和起终点引用。用户改选另一个有效面片时，面板自动读取该面片自己的参数与控制点；随后调整参数只作用于新选中的面片。
- 若没有选中有效插件面片，普通参数变化只保留面板数值并给出中文提示，不创建、删除或修改任何模型。
- 单面片原子更新失败时，该节点原有基础几何、名称、层级、变换、材质和用户属性全部保留。

验收标准：

- [x] 场景中有多个闪电面片时，选中其中一个并修改任意生成参数，仅该对象几何和 UV 更新，其他对象逐顶点和参数均不变。
- [x] 普通参数更新前后，被编辑对象的节点句柄、名称、父节点、Transform、轴心、材质和选择状态完全一致，场景中的插件面片对象数量不变。
- [x] 未选中有效插件面片时修改参数不会新建或改动模型；点击“强制重新生成”才新增一套对象并分配新名称。
- [x] 依次选择两个具有不同参数的面片时，面板会分别载入各自参数；修改第二个面片不会覆盖第一个面片的参数或几何。
- [x] 单面片生成或提交失败时原对象保持完整，无临时节点、临时名称或半成品残留。

### R-020 插件名称统一

- 插件的正式用户可见名称改为“XP闪电面片生成器”。
- 3ds Max 面板标题、版本信息、消息框标题、MZP 元数据、交付包文件名、README 和独立使用说明必须统一使用新名称。
- 当前最终交付包文件名使用 `XP闪电面片生成器_V1.4.0.mzp`。
- 场景输出对象继续使用 `LightningPlane###` 命名；`XP_LG_*` 内部标识、脚本文件名和既有用户属性保持不变，以兼容旧场景和旧面片。

验收标准：

- [x] 当前源码、MZP 元数据和使用文档中的正式用户可见名称均为“XP闪电面片生成器”。
- [x] 新 MZP 以当前版本的 `XP闪电面片生成器_V1.4.0.mzp` 交付，包内入口仍可正确加载主脚本。
- [x] 改名不改变 `LightningPlane###` 输出命名，也不破坏旧插件面片的识别和原位编辑。

### R-021 干净 Max 会话拾取兼容性

- MZP 在未加载过任何旧版脚本、没有残留 `XP_LG_*` 全局函数的干净 3ds Max 2023 会话中首次运行时，“拾取起点”和“添加终点”必须可正常进入拾取模式。
- 拾取过滤函数依赖的全部函数必须在过滤器定义前已定义，或使用 MaxScript 2023 明确支持的前向声明；不得依赖开发机旧会话中的全局函数残留。
- 拾取普通场景节点应被接受；插件自己生成的 `LightningPlane###` 面片应继续被过滤，避免把输出对象作为起点或终点。
- 此修复不得改变现有起终点列表、生成逻辑、对象命名、UV、轴心和参数编辑规则。

验收标准：

- [x] 在干净 3ds Max 2023 进程首次加载新 MZP 后，点击“拾取起点”不再出现第 84 行 `undefined` 调用错误，并可选中普通节点。
- [x] “添加终点”同样可正常拾取普通节点，同时拒绝起点本身、重复终点和插件生成面片。
- [x] 源码静态检查确认拾取过滤器不存在未定义的前向依赖，包内源码与修复源码完全一致。

### R-022 所选面片独立途经点

- 途经点只属于场景中当前唯一选中的一个 `LightningPlane` 面片；用户必须先在场景层级中选中该面片，再添加途经点。不同面片各自保存自己的途经点列表，互不共享、互不影响。
- 面板提供“添加途经点”、途经点列表、“上移”“下移”“移除所选”和“清空全部”操作。多个途经点严格按照列表顺序形成 `起点 → 途经点1 → 途经点2 → … → 终点` 的整体基础走向。
- 每个有效途经点使用所选场景对象的世界包围盒中心。生成中心线必须精确经过每个途经点中心；这里的“经过”通过改变整条路径的基础走向实现，不把途经点当作新的噪波锁定点。
- 途经点不得改变或新增噪波参数，不要求噪波在途经点处归零，不单独衰减噪波；原有路径噪波继续作用于各段，但最终中心线在途经点采样位置仍须精确落在途经点中心。
- 途经点不得重置、锁定或改变轴向 Twist，也不得改变闪电宽度、收尖、起终点锁定、随机种子、UV、轴心、命名、对象数量或其他任何现有参数语义。
- “平面面数”继续表示该面片从起点经过全部途经点到终点的总四边面数量。插件按各段基础长度确定性分配采样，并保证每个途经点对应一个中心线采样截面；若总面数不足以覆盖所有非零长度段，必须拒绝更新并给出中文提示，不得暗中增加面数。
- 添加、排序、移除或清空途经点后，只原位更新当前选中的面片，保持同一节点、名称、层级、Transform、轴心、材质和选择；其他面片不得改变。失败时保留所选面片原模型和途经点列表。
- 每个生成面片必须保存自己的有序途经点对象句柄。选择不同面片时，面板载入该面片自己的列表；旧版无途经点属性的面片按空列表兼容。
- “强制重新生成”仍按当前起点、终点新建面片，不复制其他已生成面片的途经点；新面片初始途经点列表为空。

验收标准：

- [x] 未选中唯一有效 `LightningPlane` 时不能添加途经点；选中后可以添加、查看、上移、下移、移除和清空。
- [x] 两个面片保存不同途经点列表时，切换选择会正确载入各自列表；修改其中一个不改变另一个的几何、参数或途经点。
- [x] 中心线按照列表顺序精确经过所有有效途经点中心，且总面数、连续四边面、`0–1` UV、起点轴心和对象名称保持现有规则。
- [x] 途经点只改变基础走向；相同噪波、Twist、宽度、收尖、锁端、种子等参数保持原语义，不新增或重置这些参数。
- [x] 总面数不足、途经点失效、重复相邻控制点或安全检查失败时原子拒绝，保留原面片与原途经点列表且无临时节点。
- [x] 强制重新生成的新面片途经点为空；旧版无途经点属性的面片可作为空列表继续原位编辑。

### R-023 历史版本升级至 V1.2.0

- 插件曾将正式版本号从 `V1.1.0` 升级为 `V1.2.0`；当前版本由 R-032 继续升级为 `V1.3.0`。
- 面板标题、版本标签、MZP 文件名、构建／验证脚本、测试路径、README、独立使用说明和当前交付要求必须统一使用 `V1.2.0`。
- 最终交付包文件名为 `XP闪电面片生成器_V1.2.0.mzp`；旧的 `V1.1.0` 文件不再作为当前交付包。
- 新生成面片写入 `XP_LightningGenerator = 1.2.0`；插件仍须识别并原位编辑旧版 `1.0.0` 和 `1.1.0` 面片，不改变 `LightningPlane###`、`XP_LG_*` 或其他既有用户属性名称。
- 版本升级本身不得改变 R-001～R-022 的几何、UV、途经点、实时更新、命名、轴心和原子提交语义。

验收标准：

- [x] 当前源码、窗口、标签、MZP 元数据、构建脚本、测试和文档中的当前版本统一为 `V1.2.0`。
- [x] `dist/XP闪电面片生成器_V1.2.0.mzp` 构建成功，包内源码与 `src` 一致，并可由 3ds Max 2023 加载。
- [x] 新面片写入 `1.2.0` 标记，旧 `1.0.0`／`1.1.0` 面片仍可识别和原位编辑。
- [x] 完整几何回归与中文 UI 测试通过，R-001～R-022 无版本升级回归。

### R-024 途经点坐标变化的选择同步

- 每个包含途经点的 `LightningPlane` 面片必须保存上一次成功生成时的有序控制点坐标快照；快照覆盖该面片自己的起点、全部途经点和终点世界包围盒中心。
- 用户移动途经点对象后重新唯一选中其所属面片时，插件必须比较当前控制点中心与上次成功快照；任一有效坐标发生变化时，通过现有防抖状态机自动原位更新该面片，不要求用户修改其他参数或手动点击按钮。
- 控制点坐标与快照一致时不得无条件重建，避免反复切换层级产生无意义计算、Undo 或名称变化。
- 自动同步只作用于当前唯一选中的面片，继续保持同一节点、名称、层级、Transform、轴心、材质、总面数、UV 和途经点顺序；其他面片不得改变。
- 更新成功后写入新的控制点坐标快照。更新失败时保留旧模型、旧用户属性和旧成功快照，并以简体中文说明原因，不得形成递归选择回调或连续重试。
- 旧版没有坐标快照、但保存了有效途经点句柄的面片，在首次重新选中时按当前控制点位置执行一次兼容同步；没有途经点的旧面片不因此重建。

验收标准：

- [x] 移动途经点后切换到其他对象再重新选中所属面片，模型自动原位更新并精确经过途经点新中心。
- [x] 途经点坐标未变化时反复选择该面片不会增加实时更新成功次数，也不会改变几何、名称或节点句柄。
- [x] 自动同步后所选面片的节点、名称、层级、Transform、轴心、材质、面数和 UV 保持现有规则，其他面片逐顶点不变。
- [x] 坐标变化导致面数不足、控制点重合、引用失效或安全检查失败时，旧模型和旧成功快照保持不变，且同一失败坐标不会因选择回调递归连续重试。
- [x] 旧版有途经点但没有坐标快照的面片首次重新选中时可按当前坐标同步；无途经点旧面片继续直接载入而不重建。

### R-025 途经点平滑过渡

- 面板在“所选面片途经点”区域新增“启用途经点平滑过渡”和“途经点过渡强度”参数；开关默认关闭以保持旧面片与既有场景外观，强度范围为 `0～100%`、默认 `50%`。
- 开启后，起点、途经点和终点之间的基础走向使用共享切线的受控三次 Hermite 曲线；每个途经点仍必须精确位于中心线采样截面，不得因平滑而从控制对象旁边绕过。
- 途经点进入段和离开段在基础曲线上使用同一条世界空间切线，使连接处至少达到一阶连续；切线长度必须依据前后段长度和夹角自动限制，短段、锐角和接近 `180°` 掉头时自动减弱，避免鼓包、回折或自交。
- 强度为 `0%` 或关闭开关时恢复当前逐段直线基础走向；提高强度只改变途经点附近的基础曲率，不修改途经点顺序、对象句柄、噪波／Twist／宽度／收尖／锁端／种子等参数值与职责。
- “平面面数”继续是整条面片的精确总四边面数；平滑功能不得暗中增减面数，而应继续通过曲率／Twist 自适应布线在弯曲区域分配更多现有线环，并保持连续四边面及 `0–1` UV。
- 平滑开关和强度属于每个 `LightningPlane` 自己的生成参数；选择不同面片时分别载入，调整后经现有防抖状态机只原位更新所选面片。强制新建使用当前面板值；旧版无属性面片按“关闭、50%”兼容。
- 平滑更新继续服从原子安全规则：无法在当前面数、宽度、Twist 和控制点布局下安全表达时保留旧模型和旧属性，并以简体中文提示增加面数、降低过渡强度或调整控制点。

验收标准：

- [x] 开启平滑后，典型 `90°` 途经点的基础中心线精确穿过途经点中心，且进入／离开切线方向连续，不再出现数学意义的硬折角。
- [x] 关闭平滑或强度设为 `0%` 时，几何与 R-022／R-024 当前逐段直线路径兼容；`50%` 与 `100%` 产生逐渐增强且可确定复现的过渡。
- [x] 短段、锐角及接近掉头路径的控制柄按段长和夹角受限；无法安全表达时原子拒绝并保留旧模型。
- [x] 平滑前后仍精确保持用户指定的总面数、途经点顺序、独立对象、名称、轴心和 `0–1` UV；噪波、Twist、宽度、收尖和锁端参数语义不变。
- [x] 两个面片可保存不同平滑开关和强度，切换选择正确载入；实时调整或移动途经点只更新当前所属面片，其他面片不变。
- [ ] 旧版无平滑属性的面片按关闭状态载入并可继续原位编辑；最终 V1.2.0 MZP 在 3ds Max 2023 中文版加载和运行。

### R-026 途经点平滑滑条与可选自动加面

- “途经点过渡强度”由数值微调框改为 `0～100` 的可拖动滑条，仍允许精确读取当前整数值；默认值继续为 `50`。
- 在同一区域新增“平滑时允许额外加面”选项，默认关闭。关闭时通常继续遵守 R-002／R-011／R-025；R-031 两个以上途经点的恒宽安全补面是后续明确的防畸变例外。
- 同时启用途经点平滑、过渡强度大于 `0` 且启用额外加面时，插件可主动在基础面数之外添加平滑细节；未勾选时除 R-031 安全补面外不得增加普通细节面。
- 自动加面必须集中分配到各途经点相邻的进入段和离开段，并根据转角与过渡强度确定性计算；`100` 强度时加密最明显，使曲线拥有足够截面表达圆滑过渡。直线控制点不得无意义加面，接近掉头或无法安全表达时仍服从原子安全拒绝。
- “平面面数”改为基础面数。输出面片另行保存并显示“实际面数”；实际面数等于基础面数加自动补充面数，顶点、几何面和 UV 面必须同步按实际面数生成，不得出现退化面、错误索引或手工修复需求。
- 自动加面结果必须有确定性，并设置每条分支最大实际面数保护，当前上限为 `4000`；超过上限时截断额外面数或以中文拒绝，不得导致 3ds Max 无响应。
- 自动加面开关属于每个 `LightningPlane` 自己的参数；切换选择正确载入，实时调整只原位更新当前面片并保持名称、节点、层级、Transform、轴心、材质和其他面片不变。旧版无属性面片按关闭兼容。
- 强制重新生成使用当前滑条和自动加面开关；无途经点时不得增加面数。使用说明、状态提示和预计／实际面数文字必须同步说明基础面数与实际面数的区别。

验收标准：

- [x] “途经点过渡强度”可通过滑条从 `0` 连续拖到 `100`，实时更新不重入；自动加面开关默认关闭。
- [x] 额外加面关闭时，单途经点及旧版兼容路径的实际面数等于基础面数；两个以上途经点按 R-031 可增加恒宽安全面。
- [ ] 自动加面开启且强度为 `100` 时，典型 `90°` 途经点两侧获得额外截面，实际面数大于基础面数，截图中的离散折角明显变平滑且中心线仍精确经过途经点中心。
- [x] 增加的截面集中在途经点过渡区；直线路径不加面，`50` 与 `100` 强度的额外面数和过渡精度确定性递增。
- [x] 实际面数、顶点数、几何面数、UV 面数和用户属性一致；U 仍按实际弧长从 `0` 到 `1`，所有面保持连续有效四边面。
- [x] 两个面片可保存不同自动加面状态；选择载入、实时原位更新、强制新建、失败回滚和旧面片兼容均保持现有规则。

### R-027 途经点最终中心线连续与折叠防护

- 途经点平滑不得只保证不含噪波的基础曲线连续；叠加路径噪波后的最终中心线在每个途经点也必须精确经过中心，并保证进入与离开的一阶切线连续。
- 途经点路径不得按每个直线路段重新建立互不相关的噪波横向坐标轴。整条路径必须使用连续、确定性的全局噪波场；具体按 R-028 在基础导向控制点层预补偿全局噪波，不得在途经点修改、衰减或归零噪波场。
- 自动加面只能采样已经连续的最终中心线；不得通过增加面数掩盖中心线导数跳变。新增截面继续集中在真实高曲率过渡区域并保持 R-026 的基础／实际面数规则。
- 几何安全检查除单面非退化和单面三角法线一致性外，还必须检查左右边界相对中心线的前进方向、相邻截面反转及其造成的折返风险；检测到扇形折返、边界倒退、截面反转或明显翻折时原子拒绝，不得提交截图中的近似破面结果。正常 Twist 造成的连续截面旋转不得被误判。
- 修复不得改变途经点顺序、精确中心、路径噪波参数值、Twist、宽度、收尖、锁端、种子、UV、轴心、命名、对象独立性和选择原位更新规则。

验收标准：

- [x] 启用路径噪波与途经点平滑后，典型 90° 途经点的最终中心线进入／离开切线点积接近 `1`，而不只是基础 Hermite 曲线连续。
- [x] 路径仍精确穿过途经点中心，相同参数与种子确定复现，关闭噪波时与 R-025 基础平滑曲线一致。
- [ ] 自动加面从基础面数增加到实际面数时只提高连续曲线的离散精度，不再保留固定尖角或产生途经点扇形挤线。
- [x] 左右边界倒退、截面反转、单面法线不一致或其他折叠风险时更新被原子拒绝，旧节点、名称、几何、UV 和用户属性全部保留。
- [x] 单／多途经点、自动加面开／关、Twist 开／关均保持连续有效四边面和弧长 `0～1` UV。

### R-028 途经点仅控制整体走向

- 途经点只属于基础导向曲线层，只能改变起点到终点之间的整体走向；不得在每个途经点重置、缩放、衰减、旋转或重新建立路径噪波与轴向 Twist。
- 路径噪波必须按整条分支统一的全局参数 `t=0～1`、随机种子、强度、大小和 Tiling 连续求值；加入、移动、排序或平滑途经点时，同一全局 `t` 的噪波场保持不变，不得再使用途经点局部噪波窗或局部坐标轴改变噪波表现。
- 为同时满足“最终中心线精确经过途经点中心”，应在基础导向控制点层预补偿该全局噪波值，再叠加未经途经点修改的噪波场；不得通过把途经点处噪波强制归零来实现精确经过。
- 轴向 Twist 继续只由 R-013 的整条分支全局 `t`、分段和角度计算；途经点不得重置 Twist 相位、累计角度或端点锁定逻辑。最终截面仍可按 R-012 映射到改变后的中心线切线，但同一全局 `t` 的逻辑 Twist 角必须与无途经点时一致。
- “途经点平滑过渡”和“平滑时允许额外加面”仅分别控制基础导向曲线形状和主动增加的采样密度，不得改变噪波／Twist 参数含义、宽度、收尖、UV、轴心、命名或对象更新规则；R-031 恒宽安全补面只负责防畸变。
- 本条是对 R-022“途经点只改变整体基础走向”职责边界的明确化；R-027 中“在途经点约束噪波值和导数”的实现方式作废，连续性改由“预补偿导向曲线 + 未修改的全局噪波场”共同保证。

验收标准：

- [x] 同一分支、同一随机参数下，加入或移动途经点前后，在相同全局 `t` 采样得到的噪波偏移完全一致。
- [x] 最终中心线仍精确经过每个途经点中心；平滑开启时途经点两侧一阶切线连续，且途经点处噪波不被强制压成零。
- [x] Twist 开启时，加入／移动／排序途经点前后，相同全局 `t` 的逻辑累计角完全一致，不在途经点重新开始或出现额外旋转。
- [x] 平滑开／关、自动加面开／关只改变导向曲线／采样密度；噪波参数、Twist 参数、UV、拓扑有效性和实时原位更新规则保持独立。
- [ ] 用户截图参数在新包中不再出现途经点引起的额外截面扭转或局部噪波被压平现象。

### R-029 途经点局部加面与无尖口宽度

- 自动增加的面必须形成途经点两侧的局部过渡线环，不得只是把额外面数计入整条进入段／离开段后再沿全段平均分散。每个途经点的进入侧与离开侧都必须获得明确的局部新增采样。
- 基础面继续按原有全段自适应规则分配；额外面使用独立的局部过渡窗口，强度越高窗口内采样越密。窗口内已有基础采样与新增采样必须统一重排，禁止用极小偏移错开重合采样而形成近零长度面；无关的远端直线区域不得因为某个途经点而获得这些额外面。
- 曲率安全宽度必须使用对非均匀采样稳定的三点外接圆半径估计，不得继续用“最短相邻边长度”直接估算半径；否则新增短边越多反而会把宽度错误压得越窄。
- 启用途经点平滑时，途经点曲率不得通过缩窄中间截面来掩盖急弯；途经点只改变中心路径与局部采样，面片保持用户设置的宽度。若等宽结果无法通过几何安全检查，应原子拒绝并保留旧模型，不得提交尖口、扇形面或近似破面。Twist 自身的既有安全检查继续独立执行。
- 局部加面和宽度保护不得修改 R-028 的全局噪波场、R-013 的 Twist 逻辑角、途经点中心、基础面数、UV、轴心、命名和单面片原位更新规则。

验收标准：

- [x] 对典型 `90°` 途经点，新增采样明确集中在进入侧末端和离开侧起始的局部窗口；远离途经点的区段保持基础采样分布。
- [x] 同一条圆滑曲线使用均匀或非均匀细分时，三点曲率半径和宽度缩放结果保持稳定；增加局部线环不会导致宽度进一步塌缩。
- [x] 启用途经点平滑时，途经点曲率不改变中间截面宽度；无法以等宽面片满足安全检查时拒绝更新，不生成 V 形尖口。
- [x] 实际面数、顶点数、几何面、UV 面和途经点站位继续完全一致，噪波与 Twist 参数不受影响。
- [ ] 用户两张截图中的球体途经点场景重新生成后，额外线环位于转折处，红框内不再出现尖口或扇形畸变。

### R-030 双途经点非均匀采样切线稳定性

- 一个面片包含两个或更多途经点时，中间路径段可能同时具有起始和末端局部加密；截面切线不得继续用未考虑采样参数间距的 `下一个点 - 上一个点` 长弦估算。
- 内部截面切线必须依据相邻两条实际边的归一化方向构造稳定角平分线；采样参数间距不得参与两侧方向权重，不得让较长一侧的参数跨度或弦长支配截面方向。线环距离差异不得改变同一折线路径拐点的公平分角。
- 平行运输框架和轴向 Twist 必须使用同一组稳定切线，不得各自重新猜测方向；增加、移动或排序第二个途经点不得在第一个途经点制造额外截面旋转。
- 启用途经点平滑时，Twist 安全处理不得通过把中间宽度迭代缩到尖口来提交结果；当前等宽／收尖宽度无法通过安全检查时，应原子拒绝并保留旧模型。
- 修复不得改变中心线位置、途经点精确穿心、全局噪波、Twist 逻辑角、实际面数、UV、轴心、命名和单面片原位更新规则。

验收标准：

- [x] 同一路径采用强烈非均匀采样时，内部截面切线保持进入／离开方向的公平角平分，且明显优于旧的跨点长弦方向。
- [x] 双途经点中间段同时进行首尾局部加密时，相邻切线和平行运输宽度方向连续，不出现截面突然翻转。
- [x] 双途经点与 Twist 同时启用时，安全结果保持原有中间宽度；不安全组合原子拒绝，不生成局部 1% 尖口。
- [x] 完整 Max 2023 核心／UI 回归通过，面数、四边面、UV、噪波、Twist、命名和原位更新无回归。
- [ ] 用户本轮两张截图场景在新包中不再因添加第二个途经点产生折角或局部扭曲。

### R-031 多途经点恒宽安全补面

- 途经点只改变中心路径的整体方向。除“面片两端平滑收尖”对真实起点和终点的既有影响外，任何内部途经点及其邻接过渡区域都不得改变用户设置的闪电宽度，不得出现局部缩窄、针口、V 形口或扇形挤线。
- 多途经点平滑路径不得再用 Twist 安全缩宽处理局部风险；完整内部路径宽度必须受保护。当前线环不足时，应优先在各途经点两侧局部增加四边面来表达转向，而不是修改宽度或中心线。
- 当一个面片包含两个或更多途经点并启用平滑时，安全补面属于防畸变的自动步骤，可在“平滑时允许额外加面”未勾选时按需增加实际面数；该复选框继续控制用户主动要求的额外平滑密度。安全补面不得修改面板基础面数。
- 自动安全补面必须集中在产生转向的途经点邻域，保持途经点精确穿心、全局噪波、Twist 逻辑角、收尖端点、UV、轴心、命名和原位更新规则。达到每支 `4000` 面上限仍不安全时原子拒绝，不得提交缩窄或畸变模型。

验收标准：

- [x] 两个及以上途经点、平滑开启时，所有内部截面除真实端点收尖影响外保持用户宽度，途经点及其邻接线环不再被 Twist 安全优化缩小。
- [x] 用户截图参数即使未勾选主动自动加面，也会在第二个途经点邻域按需增加实际面数，并且不再出现红框中的针口和畸变；用户于 2026-08-13 确认当前表现“还不错”。
- [x] 安全补面只提高路径离散精度，不改变中心线、噪波、Twist 逻辑角、UV、名称或当前所选面片原位更新语义。
- [x] Max 2023 核心／UI 回归通过；无法在 4000 面内保持恒宽安全表达的路径原子拒绝并保留旧模型。

### R-032 整段朝向旋转

- 面板新增独立参数“整段朝向旋转”，角度范围为 `-180°～180°`、默认 `0°`；正负值代表围绕路径切线轴的相反旋转方向。
- 该参数对整条面片的每个截面施加相同角度，只调整带状面片围绕自身中心路径的整体朝向；不得移动中心线、途经点或起终点，也不得改变宽度、基础／实际面数、局部布线、收尖、UV、轴心和命名。
- “整段朝向旋转”与 R-008／R-013 的轴向 Twist 相互独立：整段旋转是所有截面的恒定角度偏移，Twist 仍按全局长度参数累计；两者同时启用时角度相加，但任一参数值均不得被另一个功能改写。
- 当“启用整段轴向扭转（Twist）”未勾选时，整段朝向旋转的计算必须把 Twist 贡献严格视为 `0°`，不得读取或应用界面中仍保留的 Twist 分段与角度数值；单独调整朝向时整条路径每个截面的相对旋转角必须完全相同。
- 单独使用整段朝向旋转不得进入 Twist 局部缩宽优化，不得使任何截面变瘦。旋转前后每个截面左右顶点距离必须保持原值；若该恒定朝向在当前路径上无法形成有效连续四边面，应原子拒绝并保留旧模型，而不是通过缩宽提交。
- 整段朝向旋转不受“锁定起点／终点”影响；锁端仍只约束路径噪波和累计 Twist，整段恒定朝向在真实起点与终点同样生效。
- 参数必须保存到每个 `LightningPlane` 自身。选择某个面片时读取该面片值；调整时经防抖状态机只原位更新所选面片，不改名、不新建、不影响其他面片；点击“强制重新生成”才按当前值创建新对象。
- 旧版没有该属性的面片按 `0°` 兼容；`0°` 时几何必须与 V1.2.0 完全兼容。当前版本统一升级为 `V1.3.0`，并生成 `XP闪电面片生成器_V1.3.0.mzp`。

验收标准：

- [x] `0°` 与旧版几何一致；Twist 关闭但面板仍保留非零 Twist 角时，正负朝向角只产生整段相反恒定旋转，不混入累计 Twist。
- [x] 任意角度下，每个截面的中心点、宽度长度、中心线、途经点穿心、面数、UV、轴心和命名保持不变。
- [x] 与轴向 Twist 同时使用时，整段角度作为恒定偏移叠加；Twist 关闭时其分段、角度和锁端逻辑对整段朝向结果零影响。
- [x] 锁定起点／终点时，整段朝向旋转仍在相应端点生效，不被锁端包络衰减。
- [x] 两个面片可保存不同角度；选择载入与实时原位编辑只影响当前面片，旧面片默认 `0°`。
- [x] V1.3.0 源码、中文 UI、构建／验证脚本、README、使用说明和最终 MZP 版本统一，并通过 3ds Max 2023 核心与 UI 回归。

### R-033 全路径内部恒宽与弯折自动补面

- 除用户主动开启“面片两端平滑收尖”后对真实起点和终点产生的既有收尖外，整条面片所有内部截面必须始终保持用户设置的“闪电宽度”。该规则同时覆盖普通起终点路径、一个或多个途经点、路径噪波、噪波大小／强度／Tiling、轴向 Twist 和整段朝向旋转造成的弯折。
- 旧的曲率安全缩宽和 Twist 安全缩宽不得再修改任何内部截面宽度；不得通过局部缩小、针口、V 形口或把宽度倍率向相邻线环传播来提交结果。
- 当基础面数不足以等宽表达局部曲率或 Twist 时，插件必须在真实高曲率／高旋转风险位置自动增加连续四边面；新增线环按几何风险集中分配，不得只在途经点附近或沿整段平均分散。
- 自动补面不得改变中心线、噪波参数及其采样场、途经点精确穿心、Twist 逻辑角、整段朝向角、基础面数、UV 方向、轴心、名称和单面片原位更新语义。实际面数与顶点／几何面／UV 面数量必须同步更新并保存。
- 每支实际面数继续受 `4000` 上限保护；达到上限仍无法以用户宽度生成有效连续四边面时，必须原子拒绝更新、保留旧模型并给出中文提示，不得恢复缩宽方案。

验收标准：

- [x] 调整路径噪波强度、噪波大小和 Tiling 产生不同急弯时，所有内部截面宽度仍等于用户宽度；关闭收尖时包括首尾截面也保持等宽。
- [x] 轴向 Twist、整段朝向旋转及两者组合均不得使任何内部截面变瘦。
- [x] 面数不足时新增线环集中出现在真实弯折／旋转风险处，实际面数可大于基础面数，且连续四边面与 `0～1` UV 有效。
- [x] 无途经点、单途经点和多途经点路径均执行同一全路径恒宽规则，不再只保护途经点局部窗口。
- [x] 超过 `4000` 面仍不安全时原子拒绝，所选节点、名称、几何、UV、宽度和用户属性保持原状。

### R-034 噪波相位位移与统一／分轴强度

- 路径噪波新增“噪波相位位移”参数，用于沿整条分支的逻辑长度方向推进或后退噪波采样相位，使弯折图样沿路径移动；不得直接平移模型、中心线整体、起终点或途经点。
- 相位位移是确定性参数；相同控制点、随机种子、噪波参数和相位值必须复现相同结果。修改相位只改变噪波场的取样位置，不改变噪波大小、Tiling、基础走向、Twist 或整段朝向参数值。
- 新增“分开控制噪波轴向强度”开关。关闭时启用一个“统一轴向强度”倍率，同时作用于世界坐标 X、Y、Z 三个噪波偏移分量；开启时禁用统一值，分别启用“X 轴强度”“Y 轴强度”“Z 轴强度”。
- 统一及 X／Y／Z 轴向强度的最小值均为 `0`、默认值为 `1`。分轴模式允许把任意两个轴设为 `0`，只保留一个世界坐标轴方向的噪波位移；`0/0/0` 时噪波偏移精确为零，但路径噪波强度等其他面板数值保持不变。
- 轴向强度只缩放最终世界空间噪波偏移向量的对应分量，不旋转局部路径框架，不改变中心线的基础导向、途经点预补偿职责、锁端包络、Twist、整段朝向、宽度、面数、UV、轴心或命名。
- 相位、统一／分轴开关和四个强度值必须保存到每个 `LightningPlane`；切换选择分别载入，数值变化经现有防抖状态机只原位更新所选面片。旧面片默认“相位 `0`、统一控制、统一与 XYZ 强度均为 `1`”。
- 本轮正式版本统一升级为 `V1.4.0`；新面片写入 `1.4.0` 标记，同时继续识别并原位迁移 `1.0.0`～`1.3.0` 旧面片。

验收标准：

- [x] 正负噪波相位使同一确定性噪波图样沿路径前后移动，不整体平移模型；相位 `0` 与旧版结果兼容。
- [x] 统一控制时一个倍率同时作用于 X/Y/Z；分轴控制时统一值不参与结果，三个轴值分别生效。
- [x] 分轴模式设置 `X=1、Y=0、Z=0` 等组合时，噪波偏移只包含对应世界轴分量；`0/0/0` 时噪波偏移为零。
- [x] 锁定端点与途经点精确穿心继续有效，噪波相位和轴向强度不改变 Twist、整段朝向、恒宽、UV、轴心和命名。
- [x] 两个面片可保存不同相位、控制模式和轴值；旧面片按兼容默认值载入，实时编辑只更新当前面片。

### R-035 极端弯折自动重布线与归一化 UV

- 当噪波、途经点、Twist 或整段朝向产生极端弯折时，插件允许自动改变局部线环数量、线环位置和四边面的内部三角化方向，以优先生成连续、恒宽、无退化面、无翻面和无明显折叠的带状面片；不得再因固定对角线或过严的普通弯曲阈值高概率拒绝可用结果。
- 自动重布线只改变最终拓扑采样，不得改写用户设置的中心路径参数、闪电宽度、噪波参数、途经点位置、Twist 逻辑角、整段朝向角、两端收尖选项、轴心、名称和所选面片原位更新语义。
- 安全检测必须区分真实拓扑错误与可通过重布线处理的离散风险。局部风险应优先在失败区间及相邻区间插入线环，并允许在两种四边面三角化方向中选择更稳定的一种；不得继续只把整条路径面数机械翻倍。
- 自动重布线后的每个独立分支必须重新按最终中心线弧长生成贴图通道 1：起点 `U=0`、终点 `U=1`，宽度方向 `V=0～1`，所有 UV 坐标严格位于 `0～1`，几何面与贴图面数量和索引完全匹配。
- 每支实际四边面继续受 `4000` 上限保护。只有达到上限且仍存在真实退化、翻面或无法消除的局部折叠时才允许原子拒绝；提示必须包含失败类型与首个失败区段，不得只显示笼统的“4000 面失败”。

验收标准：

- [ ] 用户截图参数及一组多随机种子压力样本不再因固定对角线、普通非共面弯曲或全局翻倍策略产生高概率失败。
- [ ] 自动新增线环集中在真实风险区间，安全区间不会被无意义地整段加密；相同输入仍确定性复现相同拓扑和几何。
- [ ] 关闭收尖时所有截面保持用户宽度；开启收尖时只有真实首尾截面按既有规则变化，自动重布线不引入内部收窄。
- [ ] 最终 UV 的 U、V 均位于 `0～1`，U 沿弧长单调从 `0` 到 `1`，贴图面与最终四边面逐一匹配。
- [ ] 真实不可解案例原子保留旧模型，并显示具体失败类型和首个失败区段；不得留下临时节点或半成品。

### R-036 贴图可见圆滑度与抗折边布线

- 生成结果不能只满足“没有退化面／翻面”，还必须满足贴图可见圆滑度。中心线、左右边界和截面宽度方向在相邻线环之间不得产生肉眼明显的折边、锯齿或由非共面四边面三角化造成的贴图波浪线。
- 插件必须对相邻中心线段转角、左右边界转角、相邻截面旋转角以及四边面最佳三角化后的两三角法线差分别执行视觉阈值检查；任一项超过阈值时，必须继续在对应风险区域自动增加线环并重新采样，不能因为拓扑仍有效就提前提交。
- 视觉圆滑补面使用确定性的内部质量阈值：中心线单步转角不高于 `3°`，左右边界单步转角不高于 `15°`，截面宽度方向单步旋转不高于 `4°`；边界不得折返，短长边比不得低于 `0.25`。普通噪波／途经点弯折的最佳对角线两三角法线点积提交阈值按 R-037 调整为不低于 `0.98`。显式 Twist／整段朝向允许按用户角度形成非共面旋转，不得被抗收窄算法重写；该模式仍须通过真实退化／翻面、中心线、边界、截面旋转和折返检查。
- R-035 中“普通稳定非共面风险细分四轮后允许提交”的例外只适用于低等级拓扑预警，不得绕过本条视觉圆滑阈值。达到 `4000` 面仍无法满足视觉阈值时必须原子拒绝并保留旧模型，不得生成会让贴图产生明显波浪线的结果。
- 自动圆滑补面不得平滑或改写原始连续噪波函数、途经点导向、Twist 逻辑角、整段朝向、用户宽度、收尖、轴心或名称。未启用 Twist／整段朝向时，允许仅旋转横截面到连续的抗收窄方向，以避免恒宽平面偏移在急弯内侧形成扇形汇聚；中心线和每个截面的实际宽度必须保持不变。
- UV 继续按最终中心线弧长归一化到 `U=0～1`、宽度方向 `V=0～1`；新增线环后 U 必须严格单调，相邻 U 步长与对应中心弧长一致。双对角线选择与几何面内部三角化不得改变 UV 面的四角顺序或产生 UV 接缝。

验收标准：

- [ ] 用户截图场景及高频噪波、多途经点、Twist／整段朝向组合中，最终中心线、左右轮廓和贴图主线不再出现可见折边或周期性波浪锯齿。
- [ ] 所有成功生成分支均满足中心线 `≤3°`、左右边界 `≤15°`、截面旋转 `≤4°`、边界不折返和短长边比 `≥0.25`；普通弯折最佳三角化法线点积 `≥0.98`，显式 Twist／整段朝向保持用户旋转语义。
- [ ] 自动增加面数前后连续中心路径、用户宽度和全部生成参数保持一致；相同输入确定性复现相同实际面数和几何。
- [ ] 最终 UV 全部位于 `0～1`，U 严格单调且按最终弧长分配，V 覆盖 `0～1`，贴图面与最终四边面逐一匹配。
- [ ] 超过 `4000` 面仍无法圆滑表达时原子拒绝，中文提示包含首个视觉风险区段和超限类型，不提交折边模型。

### R-037 普通参数可靠生成与视觉检查收敛

- 起终点距离正常、未启用轴向 Twist／整段朝向且没有真实退化、翻面、零长度或边界折返的普通噪波路径，不得只因最佳三角化法线点积在 `0.98～0.99` 的轻微非共面范围内而拒绝生成。
- R-036 的普通弯折最佳法线点积提交阈值调整为 `0.98`；低于该值时仍优先局部补面。真实拓扑错误继续作为硬失败，不得因本条放宽。
- 抗收窄截面的相邻旋转限制必须按归一化路径长度计算，不得使用与线环数量绑定的固定“每截面角度”。在同一连续路径上增加面数时，截面场的总体旋转能力和外观不得随面数成倍放大。
- 自动补面必须比较连续两轮的风险数量与最低法线质量。若补面没有产生可测改善，不得继续机械膨胀到 `4000` 面；仅剩轻微、最佳对角线有效的非共面预警时应使用最佳对角线提交，真实错误或严重视觉风险仍原子拒绝。
- 普通参数生成失败提示不得再把轻微非共面描述成“无法生成有效恒宽拓扑”；提示必须区分真实拓扑错误和视觉优化未收敛。

验收标准：

- [ ] 用户截图中的单起点、单终点、100 基础面、宽度 `3`、噪波强度 `10`、噪波大小 `1`、Tiling `1`、Twist 关闭、两端锁定参数可直接生成，不再补到 `4000` 面后失败。
- [ ] 最佳法线点积为 `0.98927` 的有效四边面被视为可提交的轻微非共面，不触发原子拒绝。
- [ ] 同一路径从 100 面增加到更高面数时，抗收窄截面的累计侧倾能力保持一致，不随面数线性增大。
- [ ] 连续补面无收益时能提前停止；退化面、翻面、零长度和边界折返仍被原子拒绝并保留旧模型。

### R-038 FFD 单次生成核心重构

- 本条是对 R-010／R-011／R-035～R-037 中“生成后反复检测、循环补面、视觉阈值拒绝和内部缩宽”路线的最终替代。历史条目继续保留问题背景，但生成主链以本条为最高优先级，不得再通过修改阈值继续修补旧架构。
- 本条同时统一历史条目中的面数冲突：R-002／R-008／R-022／R-025 中“最终面数精确等于平面面数”的旧定义改为“平面面数是基础下限”；实际面数可因噪波最高频率、累计 Twist 或用户启用的途经点额外平滑面在构建前一次增加。R-031“多途经点即使未勾选也暗中安全补面”的旧例外取消；途经点额外平滑面只由面板开关控制。除这些明确覆盖点外，R-001～R-034 的工作流和参数职责继续保留。
- 每条分支必须先在逻辑空间建立规则直带：`N+1` 组横截面、每组左右两个顶点、相邻截面固定连接为一个四边面；逻辑 UV 同步建立为长度方向 `U=0～1`、宽度方向 `V=0～1`。最终几何只能由该规则直带的一次连续映射得到，不得在映射后局部拆面、焊接、缩宽或重连。
- FFD 映射职责严格分离：起点／终点／途经点／路径噪波只决定中心导向曲线；最小旋转平行运输框架决定未扭转截面；轴向 Twist 和整段朝向只旋转截面；两端收尖只缩放截面宽度。关闭收尖时所有截面宽度精确等于用户宽度。
- 中心导向曲线必须使用连续函数并按最终弧长稳定采样；多个途经点继续精确穿过中心并保持既有顺序。增加面数只能提高同一连续曲线的采样密度，不能改变噪波、途经点、Twist、整段朝向或宽度语义。
- 实际面数只能在生成前一次性解析：以“平面面数”为基础，结合噪波最高频率、累计 Twist 和用户启用的途经点额外平滑面计算确定性预算；不得根据生成后的短长边比、非共面点积或视觉阈值进入 while／重试循环。一次预算完成后只构建一次最终中心线与一次最终 Editable Poly。
- 删除生成主链中的 `4000` 面重试、短长边比拒绝、轻微非共面拒绝、中心／边界视觉转角拒绝和抗收窄二次侧倾。短长边差是弯曲 FFD 的连续拉伸结果，不属于破面；最佳有效对角线只负责确定四边面的内部显示方向，不触发重建。
- 只有输入本身无效时允许拒绝：起终点或相邻控制点重合、控制对象失效、面数／宽度／噪波数值非法、最终连续中心线出现无法建立的零长度区间、顶点或 UV 创建 API 失败。普通参数实时调整不得弹出视觉质量错误窗口。
- 性能必须与最终实际面数近似线性，禁止嵌套的全路径连续多轮优化。100 基础面的普通单分支实时更新不得出现可感知长时间冻结；测试必须记录 100／500／1000 面生成耗时和实际面数，防止重新引入无上限重算。
- FFD 映射保证固定四边面索引、非零截面宽度、连续顶点行和正确 UV；极端急弯允许内外边产生连续的自然拉伸差，但不得出现突然针口、内部缩宽、断面、错误索引或因视觉阈值而拒绝生成。
- R-001～R-034 的多终点独立对象、稳定命名、强制新建保留旧面片、所选单面片原位更新、起点轴心、途经点独立存储、中文 UI、噪波分轴、Twist 分段和整段朝向语义全部保留。

验收标准：

- [ ] 用户最近两张报错截图参数均在单次 FFD 映射中成功生成，不再出现 `0.98927` 法线点积、`0.236641` 短长边比或 `4000` 面失败提示。
- [ ] 关闭收尖时，每个内部及端点截面宽度均等于用户宽度；开启收尖时只有既有首尾包络影响宽度。
- [ ] 普通噪波、途经点、Twist、整段朝向及组合调整均保持固定连续四边面拓扑、有效顶点索引和严格 `0～1` UV，不出现针口、断面或内部缩宽。
- [ ] 同一输入确定性复现同一实际面数和几何；自动面数在构建前一次算出，源码生成主链中不存在以视觉安全为条件的 `while` 重建循环。
- [ ] Max 2023 自动回归覆盖至少 100 个随机种子、短／中／长路径、100／500／1000 基础面、单／双途经点、Twist 开关、整段朝向和收尖开关；记录成功率、最大耗时、拓扑、宽度和 UV，普通有效输入成功率为 `100%`。
- [ ] 实时参数更新不连续弹窗、不产生递归生成，100 面普通单分支操作恢复流畅；原位编辑继续保留节点、名称、层级、材质、轴心和选择。

### R-039 贴图稳定横向网格与统一对角线

- 用户截图中的深色锯齿沿四边面内部三角化方向出现。根因不是几何面被创建成三角形，而是 R-038 每个截面只有左右两个顶点，整段宽度只有一列非共面四边面；同时每个纵向四边面独立选择对角线，方向可在相邻面之间来回翻转。3ds Max、FBX 和 Unity 最终均会把非共面四边面内部三角化，因此贴图和切线插值会显露三角形分区。
- R-038 的规则直带扩展为二维规则网格：长度方向保持既有最终中心线与截面，宽度方向使用 `M+1` 个采样顶点和 `M` 列连续四边面。`M` 必须在创建 Editable Poly 前一次性确定；不得在建模后重试、缩宽或修改中心线。
- 宽度列数使用最终中心线、截面方向和收尖宽度做有界确定性规划。规划只计算虚拟四边面，不创建临时模型；依次评估有限的候选列数，并选择使三角化法线相对于相邻截面真实旋转的额外误差不超过 `0.25°` 的最小列数。用户 Twist／朝向本身产生的截面旋转不是误差，不得为了满足目标而被削弱。非近似严格共面的分支至少使用两列，使 UV 中线 `V=0.5` 落在真实纵向几何边上；规划不得形成无上限循环，宽度列和总四边面数必须有固定保护上限。
- 同一分支所有网格单元必须使用同一个对角线方向。全局方向根据整条分支两种候选方向的最差法线一致性确定；禁止逐面在 `1–3`／`2–4` 之间翻转，避免形成截图中的连续锯齿线。
- 每个宽度采样点都位于同一横截面的左右端点线性插值位置；关闭收尖时外轮廓宽度继续精确等于用户宽度，新增横向顶点不得改变中心线、Twist、整段朝向、途经点或噪波。
- UV 同步扩展为二维规则网格：U 继续按中心线弧长从 `0` 单调到 `1`，V 按宽度列从 `0` 均匀到 `1`。每个网格四边面与一个 UV 四边面严格对应，不产生接缝、反向或超出 `0～1`。
- `XP_LightningActualLongitudinalFaces` 保存长度方向实际段数，`XP_LightningWidthSegments` 保存宽度列数，`XP_LightningActualFaces` 保存最终总四边面数，`XP_LightningStableDiagonalMode` 保存整段对角线方向，`XP_LightningMaximumGridExcessAngle` 保存规划后的最大额外三角化法线误差；面板“实际面数”显示最终总数。旧面片缺少新属性时按宽度一列兼容。
- 本条对历史“Twist／整段朝向／收尖不改变实际面数”的表述作精确定义：这些参数不得改变长度方向实际段数、中心线采样或用户宽度语义；但它们造成的截面非共面程度可以使横向列数 `M` 增加，因此最终总四边面数可随 `M` 变化。横向加线只用于提高同一连续 FFD 表面的三角插值稳定性，不得反向影响任何生成参数。
- 本条不恢复 R-035～R-037 的视觉失败弹窗。若受固定总面数保护上限限制仍不能达到目标，只使用允许的最大宽度列生成，不因轻微非共面拒绝普通参数；真实零长度、非法索引或 API 创建失败仍按 R-038 原子保留旧模型。

验收标准：

- [ ] 用户本轮截图中的贴图主线不再沿内部对角线形成连续三角形锯齿，相邻网格单元不存在对角线方向交替。
- [ ] 弯曲、途经点、Twist 和整段朝向测试均按需得到 `M>1` 的横向网格；所有单元仍为有效四边面，关闭收尖时外轮廓宽度不变。
- [ ] U/V 全部位于 `0～1`，U 按最终弧长单调，V 覆盖全部横向列；几何面、UV 面、长度段数、宽度列数和最终总面数属性完全一致。
- [ ] 生成主链仍只创建一次 Editable Poly，不包含几何生成后的 `while` 重试、视觉拒绝或内部缩宽；100／500／1000 长度段性能仍记录并保持可用。
- [ ] Max 2023 核心与中文 UI 回归通过，并由用户在原贴图场景中视觉确认三角形分区消失。

### R-040 用户控制加面与规则 Plane／FFD 变形

- 本条取代 R-011／R-025 中按曲率或 Twist 重新分配既有线环的规则，取代 R-026／R-031／R-033／R-035～R-038 中主动或隐藏自动补面的规则，也取代 R-039 中按非共面程度自动增加横向列的规则。历史条目继续保留问题背景，但不得再进入当前生成主链。
- 面板新增独立整数滑条“额外加面”，范围 `0～1000`、默认 `0`。它控制每条分支在“平面面数”基础上额外增加的长度方向四边面数量；最终长度面数严格等于 `平面面数 + 额外加面`。
- 插件不得再因路径噪波强度、噪波大小、Tiling、噪波轴向强度、随机种子、途经点数量／平滑强度、Twist 分段／角度、整段朝向、收尖或四边面非共面程度暗中改变最终面数。基础 `10`、额外 `0` 时，无论上述参数如何，单分支最终必须恰好为 `10` 个四边面；基础 `10`、额外 `20` 时必须恰好为 `30` 个四边面。
- 生成顺序必须是：先在逻辑空间建立宽度一列、长度为最终长度面数的规则直线 Plane；再把起终点、途经点、噪波、Twist、整段朝向和收尖作为连续变形一次映射到这张固定拓扑。FFD 是内部变形方法，不是用户可见分段参数，也不得自行重布线、拆面、焊接、缩宽或追加横向列。
- 途经点继续只改变整体导向并精确穿心；Twist 继续只旋转截面；整段朝向继续只施加恒定截面旋转；噪波继续只改变中心导向；关闭收尖时全部截面保持用户宽度。所有参数共享同一组固定长度线环，不得互相改写。
- 每条分支只生成一列连续四边面，顶点数严格为 `2 × (最终长度面数 + 1)`，UV 顶点与几何顶点一一对应。U 按最终中心线弧长从 `0` 单调到 `1`，V 固定覆盖 `0～1`；几何面与 UV 面数量均严格等于最终长度面数。
- 四边面内部显示允许为整条分支选择一个统一对角线方向，但对角线选择只影响 Max／FBX 的内部三角显示，不得增加面数、改变顶点、拒绝普通参数或触发重建。
- 旧面片缺少“额外加面”属性时按 `0` 载入。选择面片、实时原位更新、强制新建、多终点独立对象、稳定命名、起点轴心和原子失败保护继续遵守既有规则。
- 面板实际面数显示为 `平面面数 + 额外加面`。旧的“平滑时允许额外加面”复选框及隐藏自动补面语义由本滑条统一取代，途经点平滑强度只改变导向曲线，不再改变面数。

验收标准：

- [ ] 用户截图参数（平面面数 `10`、额外加面 `0`、宽度 `4`、噪波强度 `12`、噪波大小 `1`、Tiling `0.5`、Twist `2×75°`）单分支严格生成 `10` 个四边面和 `22` 个顶点，不再生成 `528` 面。
- [ ] 额外加面滑条为 `0／10／100` 时，最终长度面数分别严格等于 `基础／基础+10／基础+100`，其他参数变化不改变该数量。
- [ ] 生成主链不再调用自动长度密度预算或自动横向列规划；每个输出的 `XP_LightningWidthSegments` 恒为 `1`，几何面、UV 面和实际面数属性完全一致。
- [ ] 噪波、单／多途经点、Twist、整段朝向、收尖及其组合只连续变形固定 Plane；无退化面、断面、错误索引、内部缩宽或生成后重布线。
- [ ] 中文 UI、选择载入、200ms 实时原位更新、强制新建、旧面片兼容、核心测试和最终 MZP 在 3ds Max 2023 中通过验证。

## 3. V1 暂不包含

- 不自动创建 V-Ray、Corona、Arnold 或其他特定渲染器材质。
- 不包含贴图文件制作和闪电发光着色器。
- 不包含动画、电流流动或自动生长效果。
- 不包含未经用户确认的递归小分叉系统。
- 不包含体积闪电或必须依赖第三方插件的效果。

以上项目可在后续版本单独追加，不得在未确认时取代 V1 必需功能。

## 4. 强制开发与遗漏检查流程

每一次开发迭代都必须执行以下流程：

1. 开发前完整读取本文件，确认本轮修改对应的需求编号。
2. 开发过程中不得静默删除、弱化或改变已确认需求；发现冲突时先停止并向用户确认。
3. 开发完成后再次完整读取本文件。
4. 将本轮实现逐项映射到 `R-001` 至 `R-040`，检查是否引入需求遗漏或回归。
5. 执行当前环境允许的静态检查、脚本加载检查和 3ds Max 实机验证。
6. 只有存在验证证据时才勾选验收项；未验证项保持未勾选，不得推测为通过。
7. 在“开发验证记录”中追加本轮结果，并明确写出未完成、未验证和已知问题。
8. 向用户汇报时必须说明：本轮完成内容、验证方式、遗漏项和下一步。

## 5. 开发验证记录

| 日期 | 迭代 | 对应需求 | 验证结果 | 未完成／未验证／已知问题 |
| --- | --- | --- | --- | --- |
| 2026-08-07 | 需求基线 | R-001～R-006 | 已记录并核对用户确认的 V1 需求；尚未开始插件开发 | 所有实现类验收项均待开发与实机验证 |
| 2026-08-07 | V1.0.0 首次开发 | R-001～R-006 | 已在 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe`（2023.2.2 中文版）通过核心实机测试：单／多终点、移动控制点、失效引用清理、每支 1／8／64 个真四边面、三终点 192 面、拓扑连续、UV 通道 1 与逐面索引、宽度和噪波参数、端点锁定与平滑包络、确定性、10 次重建轴心、非法输入原子保护、MZP `run` 与 `drop` 提取路径；最终结果为 `artifacts/gui-smoke-result.txt = PASS`。成品 MZP 在全新 Max 2023 进程中成功打开中文 rollout，25 个控件通过 HWND／可见性／标题／默认值检查，`artifacts/ui-smoke-final.txt = PASS`。静态包检查通过，最终包仅含 `mzp.run` 和主脚本。 | 尚未用物理鼠标从 Windows 资源管理器执行最后一次拖放；已在干净 Max 进程验证 MZP `run`，并通过 Max 2023 的 `msZip.unloadPackage` 验证 `drop` 指向的主脚本可提取和加载。极端参数下不同分支之间的视觉交叉属于形状结果，未做全局自相交消除。 |
| 2026-08-12 | V1.1.0 轴向 Twist 更正与实时更新 | R-001～R-008 | 已在用户指定的 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe` 中通过完整核心实机回归，`artifacts/gui-smoke-result.txt = PASS`：轴向 Twist 通过绕每个中心线采样点局部切线旋转宽度方向实现，开关／分段／角度不改变左右顶点中点形成的细噪波中心线；正负角度方向相反，角度 0 与关闭一致；起终点锁定角精确为 0 并平滑衰减；平面面数 1 且两端锁定时无可见 Twist；三终点在每支 1／8／64 面下拓扑、UV 和面数正确；相同输入确定复现，随机种子只改变细噪波中心线。R-007 通过显式参数更新、50 次快速请求合并、真实 200ms Timer Tick、非重入深度 1、失败保留旧模型及中文状态验证。最终 MZP 在全新 Max 2023 进程中打开中文 rollout，29 个控件通过 HWND／可见性／中文标题／默认值／正负角度范围检查，`artifacts/ui-smoke-final.txt = PASS`；静态包检查通过且包仅含 `mzp.run` 与主脚本。 | 尚未用物理鼠标从 Windows 资源管理器执行最终拖放，因此 R-006 第一项保持未勾选；已通过 MZP `run` 和 `drop` 提取／加载验证。为保证不生成破面，平面面数过低且轴向角度或分段过激时会原子拒绝更新并给出中文调整提示；未实现不同分支之间的全局自相交消除。 |
| 2026-08-12 | 两端平滑收尖增量 | R-001～R-009 | 已在用户指定的 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe` 中通过完整核心回归，`artifacts/gui-smoke-result.txt = PASS`：默认关闭保持既有几何；开启后每条分支起终点宽度为中段的 1%，端部宽度按 smootherstep 平滑过渡，中段保持用户宽度；收尖前后中心线和 UV 数组一致；与轴向 Twist 同时启用仍保持有效四边面；三终点每支 1／8／64 面的面数、拓扑和 UV 正确；实时切换收尖会自动重建且不改变中心线。最终 MZP 在全新 Max 2023 进程中打开中文 rollout，30 个控件通过 HWND／可见性／中文标题／默认关闭状态检查，`artifacts/ui-smoke-final.txt = PASS`。静态包检查通过且包仍只包含 `mzp.run` 与主脚本。 | 端点为防止退化面而保留正常宽度的 1%，不是数学意义的零宽焊点；平面面数为 1 时没有中间截面，只生成一张两端均为 1% 宽度的窄四边面。物理鼠标拖放仍未执行，因此 R-006 第一项保持未勾选。 |
| 2026-08-12 | 独立分支对象与 Twist 抗折叠优化 | R-001～R-010 | 已在用户指定的 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe` 中通过 175 项核心实机断言，`artifacts/gui-smoke-result.txt = PASS`：每个终点生成一个独立 Editable Poly 对象；三终点在每支 1／8／64 面下对象数、逐对象面数、连续拓扑、贴图通道 1、独立 `0–1` UV 和公共起点轴心均正确；添加、移除、重排终点和实时更新会整组创建新对象。生成器以每面相邻 Twist 最大 `45°` 和两三角区域单位法线点积至少 `0.70` 作为局部安全阈值；低面数／720° 案例被中文提示原子拒绝，两个旧分支对象均保留且无临时节点；120°／32 面安全案例正常通过。最终 MZP 在全新 Max 2023 进程中通过 41 项 UI 断言，30 个控件和 Twist 安全默认角 `90°` 正确，`artifacts/ui-smoke-final.txt = PASS`；静态包检查通过且包仅含 `mzp.run` 与主脚本，SHA256 为 `BFE00D8C238BF0908BD32D0A505C8FEFC2D49CFD725EDED905141004F45DD3B4`。 | 本轮解决的是单个分支的局部四边面翻折／非共面三角条纹；仍未实现同一分支远距离区段之间或不同独立分支对象之间的全局自相交消除。物理鼠标拖放仍未执行，因此 R-006 第一项保持未勾选；MZP `run`／`drop` 加载和全新 Max 进程 UI 已通过。 |
| 2026-08-12 | 曲率／Twist 自适应布线 | R-001～R-011 | 已在用户指定的 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe` 中通过 182 项核心实机断言，`artifacts/gui-smoke-result.txt = PASS`：生成器先以每支面数四倍且至少 64 段的高密度导向线估计曲率与 Twist 变化，再把用户指定的精确 `N` 个四边面按权重重新分配；急弯处依据局部曲率半径自动降低宽度，并以每截面最大 `0.12` 的倍率变化向两侧平滑传播。直线宽度保持 100%，人工 90° 急弯会触发收窄；同一输入的采样点和参数可确定复现。用户截图参数 100 面／宽度 3／噪波 15／大小 0.5／Tiling 1／种子 1／Twist 3 段 75°／两端锁定已成功生成 100 个四边面、202 个顶点，通过四边面、内外轨前进方向、R-010 法线与 `0–1` UV 检查，不再只保留旧模型。最终 MZP 在全新 Max 2023 进程中通过 41 项 UI 断言，`artifacts/ui-smoke-final.txt = PASS`；包结构与源码一致，SHA256 为 `8A083E8843291727D1593BF1DD56ED56C558F6F59BAA1C718452455E210E636F`。 | 自适应布线处理局部曲率和 Twist 挤线；相隔较远的非相邻区段或不同独立分支对象之间的全局空间自相交仍不在本轮范围。物理鼠标拖放仍未执行，因此 R-006 第一项保持未勾选。 |
| 2026-08-12 | FFD 式逻辑空间 Twist | R-001～R-012 | 已在用户指定的 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe` 中通过 187 项核心实机断言，`artifacts/gui-smoke-result.txt = PASS`：未扭转截面改由相邻切线间最小旋转轴进行平行运输，弯曲测试路径相邻框架无翻转；Twist 先在 `0–1` 直线逻辑空间按累计角度计算，再围绕运输后的局部切线映射。直线与弯曲路径在同一逻辑位置的基准／扭转方向点积均等于目标角余弦，证明曲率未放大角度。两端锁定的 120° 测试在 0.25／0.5／0.75 位置保持累计趋势，中点低于 80°，不再使用中段达到满角的正弦峰值；起终点仍精确归零。0°、正负角度、端点锁定、截图 75° 参数、单／多终点、1／8／32／64／100 面、动态布线、UV、实时更新与原子失败保护均回归通过；输出记录 `XP_LightningTwistMapping=FFDParallelTransport`。最终 MZP 在全新 Max 2023 进程中通过 41 项 UI 断言，`artifacts/ui-smoke-final.txt = PASS`；包结构与源码一致，SHA256 为 `36F50AF2E7D81CAAA162060D33077A0ACB99AC6D044FEFB87E4E38EF40A6B092`。 | 相隔较远的非相邻区段或不同独立分支对象之间的全局空间自相交仍不在本轮范围。物理鼠标拖放仍未执行，因此 R-006 第一项保持未勾选。 |
| 2026-08-12 | 等距逐段 Twist 与稳定命名 | R-001～R-014 | 已在用户指定的 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe` 中通过最终完整核心实机回归，`artifacts/gui-smoke-result.txt = PASS`：`K=2` 精确三等分并在前两段各累计一次角度；未锁定终点时 `K=10`、每段 120° 最终累计 1200°；锁定终点在最后等长区间按最短等价余角回正。有效段改用恒定线性角速度，避免 easing 在段中放大旋转速度；高 Twist 非共面风险通过局部宽度迭代收窄和平滑传播处理。50 面、10 段、每段 120°、两端锁定、双终点参数成功生成两个各 50 四边面的独立对象并通过拓扑、法线与 `0–1` UV 检查。空场景分支依次为 `LightningPlane001～003`；参数重建按终点句柄继承旧名，手动改名、移除／新增终点和原子失败名称保护均通过。最终 MZP 在全新 Max 2023 进程中通过中文 UI 测试，`artifacts/ui-smoke-final.txt = PASS`，30 个控件已显示。 | 物理鼠标从 Windows 资源管理器执行最终拖放仍未人工操作，因此 R-006 第一项保持未勾选；已通过 MZP `run`／`drop` 加载。相隔较远区段或不同独立分支间的全局空间自相交仍不在本轮范围。 |
| 2026-08-12 | 强制新建组与 Unity 轴向适配 | R-001～R-016 | 已在指定的 3ds Max 2023 中通过最终完整核心实机回归，`artifacts/gui-smoke-result.txt = PASS`：连续两次强制重新生成均保留旧组并各新增一套 `LightningPlane###`，场景累计三组；普通参数更新仅原子替换最新活动组，历史组名称和逐顶点世界坐标不变；高风险强制新建失败时活动组、历史组和对象总数保持原状且无临时节点；清除当前输出只删除活动组。Unity 操作将活动对象局部矩阵设为 X 不变、Y 对齐原世界 Z、Z 对齐原世界 -Y，逐顶点世界坐标、UV、名称、面数及起点轴心保持不变；历史组不受影响，重复操作幂等，一次 Undo 可恢复，实时替换会继承已适配状态。最终 MZP 在全新 Max 2023 进程中通过 UI 测试，`artifacts/ui-smoke-final.txt = PASS`，31 个控件包含“一键适配 Unity 轴向”按钮。 | 物理鼠标拖放仍未人工操作，因此 R-006 第一项保持未勾选；MZP `run`／`drop` 已验证。Unity 左手坐标系的最终 FBX 导入选项仍由用户的导出器和 Unity Import Settings 决定，本功能负责生成对象局部 Y-Up 轴向表达。 |
| 2026-08-12 | Unity 轴向烘焙与零旋转导出 | R-001～R-017 | 已在指定的 3ds Max 2023 中通过最终完整核心实机回归，`artifacts/gui-smoke-result.txt = PASS`：活动组按无镜像旋转 `(x,y,z)→(x,z,-y)` 烘焙到 Editable Poly 顶点，矩阵行列式为 `+1`；节点 Transform 旋转部分恢复单位矩阵，Rotation 为 `0/0/0`、Scale 为 `100%/100%/100%`，公共起点轴心保持不变。逐顶点映射、UV、面数、拓扑、名称和材质均通过断言；旧版 X=90° Unity Axis 对象可自动迁移；重复操作幂等、一次 Undo 可恢复，普通参数实时重建继承烘焙状态并继续保持零旋转。最终 MZP 在全新 Max 2023 进程中通过 UI 测试，`artifacts/ui-smoke-final.txt = PASS`，31 个控件包含“烘焙 Unity 轴向（旋转归零）”按钮。 | 物理鼠标拖放仍未人工操作，因此 R-006 第一项保持未勾选；MZP `run`／`drop` 已验证。FBX 导出器和 Unity Import Settings 属于外部导出／导入设置，本插件保证生成对象本身为无镜像烘焙且节点旋转归零。 |
| 2026-08-12 | Unity 参数归零且模型外观不动修复 | R-017；R-018 暂停 | 已把直接顶点烘焙改为补偿式对象轴向：节点 Transform 保持单位旋转，内部 Object-Offset 保存无镜像 Y-Up 旋转，局部顶点使用逆矩阵补偿；按 Autodesk 2023 官方关系 `Object Transform = Object-Offset × Node Transform` 复核，并按矩阵／旋转属性方向约定对四元数取逆。新增测试断言覆盖适配前后逐顶点世界坐标完全一致、Rotation `0/0/0`、Scale `100%`、内部轴向、名称／拓扑、幂等、Undo、实时继承、旧 X=90° 迁移，以及 `BakedYUpV2` 错误转向恢复。静态包检查通过，最终包仅含 `mzp.run` 与主脚本。 | 第二个 3ds Max 2023 GUI 进程被系统拒绝启动；随后 `3dsmaxbatch.exe` 因 `ADLSDK_STATUS_LICENSE_NO_CHECKED_OUT` 未取得许可，未执行到探针脚本。因此本轮新的 R-017 验收项保持未勾选，旧 `PASS` 文件仅代表被本轮取代的直接烘焙版本，不能作为本修复证据。用户要求先暂停 `插件使用说明.md`，R-018 暂不验收。 |
| 2026-08-12 | Unity 轴向 `rotationPart` 运行错误修复 | R-017 | 根据用户提供的 MaxScript 异常窗口定位到 `src/XP_LightningGenerator.ms` 原第 1164 行：错误地把 Matrix3 的 `rotationPart` 当作全局函数调用，导致“调用需要函数或类，得到的是 undefined”。已改为 Max 2023 支持的实例属性访问 `inverse (unityAxisRotation.rotationPart)`；全项目扫描确认没有其他 `rotationPart` 调用，静态包结构与源码一致性验证通过。 | 当前唯一 Max 许可证正在用户实例中使用，无法启动隔离测试实例；本修复需用户重新载入 MZP 后点击按钮复验，R-017 仍保持未勾选。说明 MD 继续暂停。 |
| 2026-08-12 | Y-Up 真实轴心与冻结零旋转修复 | R-017；R-018 暂停 | 已废弃 Object Offset 模拟方案：节点本地轴改为 `X=[1,0,0]`、`Y=[0,0,1]`、`Z=[0,-1,0]`，再按最终 Object Transform 逆算 Editable Poly 局部顶点，确保适配前后世界顶点不动；旋转控制器按 Max 2023 内置 `Macro_FreezeTransform.mcr` 的方式建立 Rotation List，第一层保存 Y-Up 轴心基准，第二层为活动的零值 Euler XYZ。新增／更新测试覆盖世界顶点、Y-Up 本地轴、活动 `0/0/0`、Object Offset 单位化、幂等、Undo、实时继承和 `BakedYUpV2`／`CompensatedYUpV3`／旧 X=90° 迁移。PowerShell 包验证、UTF-8 BOM、包内源码一致性、必要令牌与 MaxScript 已知静态语法陷阱扫描均通过；MZP SHA256 为 `EBC35E3C6070959A1BD777D451713172A8F9AB52EC8D906A8630B167B087ACC5`。 | 当前用户的 Max 2023 实例占用唯一许可证，未启动第二实例执行 `fileIn`／smoke；R-017 验收项继续保持未勾选，需用户重新拖入新版 MZP 后实机确认轴心 Y-Up、旋转栏 `0/0/0` 且模型外观不动。按用户指示继续暂停 `插件使用说明.md`。 |
| 2026-08-12 | 选中面片原位参数编辑 | R-007、R-014、R-015、R-017、R-019；R-018 暂停 | 普通参数请求现在必须捕获场景中唯一选中的单个插件面片；无有效选择或同时选择多个插件面片时只保留面板值并提示，不生成对象。每个新面片新增保存面数、宽度、路径噪波、Noise Size、Tiling、种子、锁端、Twist 与收尖参数；选择变化回调会载入该对象的参数和起终点引用。更新时先创建并验证临时分支，将世界顶点换算到目标节点局部空间，再使用 Autodesk 2023 官方 `instanceReplace` 保留目标节点身份、名称、父子层级、Transform、轴心、材质和选择；备份节点用于异常恢复，成功／失败均清理临时节点。起终点列表操作不再自动生成或删除已有面片，只有“强制重新生成”新增对象。测试已扩展为未选择不生成、同节点句柄／名称／父节点／变换／对象数保持、未选分支不变、切换选择载入独立参数。静态包结构、源码一致性、UTF-8 BOM、必要令牌和已知 MaxScript 语法陷阱扫描通过；MZP SHA256 为 `4CE2E945904E6E0135673E44F5BDBC76A94225AEBD7183AB41C002AA6DB83B02`。 | 当前 Max 2023 许可证由用户实例占用，未执行本轮 `fileIn`／完整 smoke，因此 R-019 验收项保持未勾选。需用户重新拖入新版 MZP，验证选择切换、原位编辑与强制新建。说明 MD 继续按要求暂停。 |
| 2026-08-12 | 撤销 Unity／Y-Up 轴向修改功能 | R-005、R-006、R-016、R-017；R-018 暂停 | 已从主脚本删除轴向按钮、事件、`XP_LG_ApplyUnityAxisToNode`、冻结旋转函数、`Rotation_list()` 创建、`XP_LightningUnityAxis` 标记写入及重建继承逻辑；新测试反向断言新建／原位更新不得出现插件轴向标记或 Rotation List。UI 测试删除旧按钮定义并增加按钮不存在断言；专用旧轴向探针已删除。静态分隔符、MaxScript 已知陷阱、UTF-8 BOM、包内源码一致性和禁止令牌检查均通过；最终 MZP 仅含 `mzp.run` 与主脚本，SHA256 为 `6751CABAB70C82CD903A29B07E6A8F6EE876B75DA1F1C58CA4D0BC1947FEFC17`。 | 独立 3ds Max 2023 GUI 测试实例被系统以 Access Denied 拒绝启动，因此本轮未取得新的 `fileIn`／核心 smoke／UI smoke／FBX 导出证据；除“源码和包中按钮入口不存在”外，其余 R-016／R-017 新验收项保持未勾选。旧版本已改轴向的场景节点不会在加载时自动修改，需要强制重新生成新面片后再验证 FBX。按用户指示继续暂停 `插件使用说明.md`。 |
| 2026-08-12 | 恢复并重写独立中文使用说明 | R-018；对照 R-001～R-019 | 已将 `插件使用说明.md` 重写为当前工作流：第一批模型必须点击强制重新生成；普通参数只原位更新场景中唯一选中的插件面片；多终点强制生成后需单独选择一条分支再编辑；控制点列表操作不修改已有面片。说明覆盖当前 19 个用户可操作控件、8 组数值范围与默认值、多终点独立对象、稳定命名、原子失败保护、UV、起点轴心、路径噪波、FFD 式 Twist、`K+1` 分段、收尖和动态布线；已删除旧的自动生成、整组实时替换和可用 Unity／Y-Up 轴向按钮描述，并补充旧 Rotation List 的 FBX 提示。自动文本核对确认全部控件和范围存在，废弃工作流短语不存在。 | 本轮只修改说明和需求记录，未修改 MaxScript 或 MZP，因此无需新增 3ds Max 实机回归；R-016／R-017 与 R-019 原有待实机验收状态不变。 |
| 2026-08-12 | 插件正式改名为 XP闪电面片生成器 | R-006、R-018、R-020；对照 R-001～R-020 | 面板标题、版本标签、消息框标题、MZP `name`／`description`、README、独立使用说明、构建／验证脚本和测试路径均已同步；新包 `dist/XP闪电面片生成器_V1.1.0.mzp` 构建成功，仅含 `mzp.run` 与 `XP_LightningGenerator.ms`，包内源码 SHA256 与 `src` 完全一致。静态检查确认源码 UTF-8 BOM、引号与圆／方括号平衡，且无顶层 `local`、复合赋值或行首续接 `+`；新包 SHA256 为 `2347C8862F35C5E68926C99DF104F759ECF8768D5CEAB0B3958625025BE33775`。 | 本轮未启动 3ds Max 2023，因此新面板标题显示和旧场景面片原位编辑兼容性仍待实机复验；内部 `XP_LG_*`、用户属性、主脚本文件名及 `LightningPlane###` 规则未改，既有 R-016／R-017 与 R-019 待验收状态不变。 |
| 2026-08-12 | 干净会话拾取函数前向引用修复 | R-001、R-006、R-018、R-019、R-021；对照 R-001～R-021 | 根据他人电脑截图定位到主脚本第 84 行：`XP_LG_PickFilter` 在 `XP_LG_IsManagedOutput` 定义前调用它，开发机旧会话的全局函数残留掩盖了问题。已把被依赖函数移到过滤器前，并通过全脚本定义顺序扫描发现及修复 `XP_LG_CaptureNodeWorldVertices` 的第二处同类前向引用。验证脚本新增所有 `XP_LG_*` 函数前向引用扫描，结果为 0；核心测试新增普通节点可拾取、插件输出面片被过滤的断言。修复 MZP 仅含入口与主脚本，UTF-8 BOM 和包内源码一致性通过，SHA256 为 `90AFCB22483B5DB52A86DBC0E454B6428D58ECDE083DD67DFE8759505B066D64`。使用说明补充第 84 行错误处理，README 同步纠正控制点工作流。 | 隔离 3ds Max GUI 进程被系统以 Access Denied 拒绝启动；`3dsmaxbatch.exe` 也未在 60 秒内完成并已终止本轮启动的测试进程，因此没有新的干净 Max 实机 PASS。需在对方电脑关闭旧窗口后重新拖入修复包，复验“拾取起点”“添加终点”和单面片参数更新。 |
| 2026-08-12 | 途经点开发前稳定备份 | R-001～R-021；R-022 待确认 | 已创建 `backups/2026-08-12_pre-waypoint_R021`，包含完整源码 ZIP、当前修复版 MZP 和备份清单；备份 MZP SHA256 与 `dist` 原包完全一致，均为 `90AFCB22483B5DB52A86DBC0E454B6428D58ECDE083DD67DFE8759505B066D64`。源码 ZIP SHA256 为 `C89DF4B67F2F3DAF66B7D505F6F15F1119FFE74E58333AC89966A33AE6263D70`。 | 途经点的多终点归属、顺序管理和精确经过／噪波锁定语义仍待用户确认；确认前未修改生成器代码和当前 MZP。 |
| 2026-08-12 | 所选面片独立途经点实现与验证 | R-001～R-022 | 已实现按所选 `LightningPlane` 独立保存的有序途经点、添加／上移／下移／移除／清空 UI、按基础段长确定性分配总面数、每段自适应布线、精确途经截面、对象级原位更新和失败回滚；旧版无属性按空列表兼容，强制新建始终为空列表。`tools/build-package.ps1` 与 `tools/verify-package.ps1` 通过，前向内部函数引用为 0；3ds Max 2023 完整几何回归 `artifacts/gui-smoke-result.txt` 于 20:49:45 写入 `PASS`，专项断言覆盖途经点顺序、排序／清空提交、中心精度、总面数、四边面、UV、对象隔离、重复／失效引用与面数不足的原子失败，以及强制新建空列表；最终中文 UI 测试 `artifacts/ui-smoke-waypoint-r022.txt` 写入 `PASS`，窗口共 39 个控件；最终 MZP SHA256 为 `36AB1A5AEAF3D7C3376CD15AA2DB08A366B463A478DE375390D0D731AFD4594D`。 | 仍建议用户在自己的复杂制作场景中人工检查尖锐折线路径的视觉宽度变化；当前自动安全算法会在必要时局部收窄，属于既有防折叠规则。 |
| 2026-08-12 | 当前版本升级至 V1.2.0 | R-001～R-023 | 已统一主脚本窗口／标签、MZP 元数据、构建与验证脚本、测试路径、README、独立使用说明和当前交付要求；新面片写入 `1.2.0`，识别逻辑继续支持 `1.0.0`／`1.1.0`。最终包 `dist/XP闪电面片生成器_V1.2.0.mzp` 仅含入口与主脚本，包内源码一致性和内部函数前向引用检查通过，SHA256 为 `36ABB78727154D4C9D7C0458B7D1FC3FFAB6F30D11F2904E20F6DE3069D0907D`。3ds Max 2023 完整几何回归于 20:59:43 写入 `PASS`，包含旧标记面片原位编辑并迁移到 `1.2.0` 的断言；中文 UI／最终 MZP 测试 `artifacts/ui-smoke-v120.txt` 于 21:00:12 写入 `PASS`，窗口标题和版本标签为 V1.2.0，共 39 个控件。 | 仍未用物理鼠标从资源管理器拖放最终包，且未执行 FBX 静态导出；对应 R-006 与已撤销轴向功能的两个历史人工验收项继续保持未勾选。 |
| 2026-08-12 | 途经点坐标选择同步优化 | R-018、R-019、R-022、R-024；对照 R-001～R-024 | 面片新增长期保存的 `XP_LightningControlSnapshot`，包含起点、有序途经点和终点的句柄与世界中心；重新选择时只在当前签名不同于上次成功签名时通过现有 200ms 状态机排队原位更新。成功提交随临时 Editable Poly 原子复制新快照；失败保留旧用户属性并记录本次失败签名，避免同一失败坐标由选择回调连续重试。旧版有途经点但无快照时会兼容同步一次，无途经点时不触发。核心测试已增加移动后切回、未变化不重建、失败保留与抑制、恢复及旧版迁移断言；README 与 `插件使用说明.md` 已同步。构建／验证脚本通过：UTF-8 BOM、内部函数前向引用 0、包内源码一致、仅含两个预期文件，最终 MZP SHA256 为 `41D9D6AC09DBD1622040798E819AB04DAB6F83B669A454AB19FB66017DAEC527`。 | Windows 拒绝启动独立 Max GUI；`3dsmaxbatch.exe` 因当前 Max／许可证占用在 60 秒内未进入测试并已终止本轮启动的 PID，只保留用户原有 Max 进程。因此本轮新增 R-024 实机验收项保持未勾选，需在用户当前 Max 重新载入 MZP 后复验。 |
| 2026-08-12 | 途经点共享切线平滑过渡 | R-018、R-019、R-022、R-024、R-025；对照 R-001～R-025 | “所选面片途经点”新增默认关闭的平滑开关与 `0～100`、默认 `50` 的强度。基础走向使用弦长参数化的三次 Hermite 曲线；相邻段复用同一世界切线，切线经前后位移向量和参数跨度自动受短段长度及夹角限制，接近掉头时趋近于零。关闭或零强度严格回到逐段直线；途经点截面仍精确落在对象中心。参数已贯穿单面片原位替换、强制新建、用户属性复制、选择载入、200ms 实时更新和旧面片默认值；自适应布线继续保持精确总面数与 `0–1` UV，安全失败中文提示包含降低过渡强度。核心／UI 测试已增加共享切线、90°、50%／100%、短段、掉头、属性隔离、选择载入、面数／拓扑／UV 与控件状态断言；README、`插件使用说明.md` 和包验证令牌已同步。构建与静态验证通过：UTF-8 BOM、内部函数前向引用 0、包内源码一致、仅含两个预期文件，主脚本／测试圆方括号差均为 0；最终 MZP SHA256 为 `4E7ECBEAD720CB2CAC593F706237CB657E9FE35C14353D309112F56A4789A277`。独立数学复核的 90° 连接切线点积为 `0.999992`，中点产生受控偏移。 | 已按用户指定尝试 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe`，但当前执行通道未产生新的测试结果文件；此前误用的同目录 `3dsmaxbatch.exe` 明确以 `ADLSDK_STATUS_LICENSE_NO_CHECKED_OUT` 退出。因此 R-024／R-025 新增实机验收项全部保持未勾选，旧 `PASS` 文件不能作为本轮证据；需在可取得许可证的 Max 2023 会话重新拖入当前 MZP 后执行几何与 UI 复验。 |
| 2026-08-12 | 途经点平滑滑条与可选自动加面 | R-018、R-019、R-022、R-024～R-026；对照 R-001～R-026 | “途经点过渡强度”已改为 `0～100` 整数滑条；新增默认关闭的“平滑时允许自动加面”和基础／实际面数提示。自动补面仅在平滑开启、强度大于 0 且用户显式勾选时生效，按途经点转角与强度确定性分配到进入／离开相邻段；共线点不补面，每支实际面数封顶 `4000`。生成器按实际点数创建顶点、连续四边面和弧长 `0～1` UV，同时分别保存 `XP_LightningFacesPerBranch`、`XP_LightningActualFaces` 与 `XP_LightningWaypointAutoAddFaces`；旧面片默认关闭并维持基础面数。核心测试新增关闭兼容、50／100 递增、90° 两侧补面、直线不补面、精确经过中心、拓扑／UV／属性一致及选择载入断言；UI 测试新增滑条、默认状态和从属启用断言。README、`插件使用说明.md`、需求与包验证令牌已同步。构建和静态验证通过：UTF-8 BOM、内部函数前向引用 0、包内源码一致、仅含两个预期文件，主脚本／测试圆方括号差均为 0；最终 MZP SHA256 为 `58B87273A3F62CB34236017A68FAD31DFE3D0E43505728384F71EEC55CE223E6`。 | 已仅调用用户指定的 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe` 尝试执行几何回归，但进程很快返回且没有更新结果文件；现有 `artifacts/gui-smoke-result.txt` 是本轮之前的旧失败记录，不能作为证据。等待式启动又被 Windows 以 Access Denied 拒绝。R-024～R-026 实机验收项继续保持未勾选，需在可用 Max 2023 会话重新载入当前 MZP 后人工观察 100 强度折角并执行核心／UI 回归。 |
| 2026-08-12 | 途经点最终中心线连续与折叠修复 | R-018、R-019、R-022、R-024～R-027；对照 R-001～R-027 | 已定位两个共同根因：旧路径噪波在每个途经区间重新建立互不连续的侧向轴，导致基础 Hermite 曲线虽然连续、最终含噪中心线仍只有位置连续；原途经点切线实际使用未限幅的弦向量，短段／大夹角会产生过冲。平滑模式现改为全路线稳定参考轴投影到局部切线法平面，并使用端点值和一阶导数均为零的确定性噪波窗；途经点切线改为受相邻短段、夹角和参数跨度共同限制的共享角平分线。自动加面只对修复后的连续曲线重采样；关闭平滑或强度为 0 仍走旧逐段噪波路径。安全检查继续拒绝边界倒退、截面反转和单面法线不一致，同时不误伤正常 Twist。用户指定的 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe` 核心回归于 22:43:06 写入 `artifacts/gui-smoke-result.txt = PASS`；新增断言覆盖 90° 途经点最终含噪切线点积 `>0.995`、精确经过中心、确定复现、自动加面、Twist、连续四边面、UV、控制点坐标同步与截面反转拒绝。最终构建／静态包验证通过，主脚本和测试圆／方括号差均为 0，MZP SHA256 为 `C7DA654DD6D8524816927E96250A65A9BA386D0848FF78617F00305F6ED140EA`。 | 自动 UI 测试本轮未生成新的结果文件，不作为完成证据；用户截图已证明新增控件可显示，但最终 MZP 的 UI 自动回归仍待补跑。用户截图中的复杂手部路线需要用本轮新包重新生成后进行最终视觉复验，因此 R-026／R-027 对应视觉项保持未勾选。 |
| 2026-08-12 | 途经点导向职责隔离修复 | R-004、R-008、R-012、R-013、R-022、R-025～R-028；对照 R-001～R-028 | 已撤销上一轮在每个途经点使用局部噪波轴和归零噪波窗的实现。新算法只用起终点建立整条分支稳定噪波轴，并始终按全局 `t=0～1` 原样计算噪波；内部途经控制点在基础导向层减去该全局噪波值，最终叠加噪波后精确回到对象中心。共享 Hermite 切线由预补偿后的导向点计算，平滑开关只决定直线／平滑导向；Twist 仍只调用 R-013 全局累计角函数，再按 R-012 平行运输映射到最终中心线。Max 2023 首次完整运行中，R-028 新增专项断言全部通过：途经点处原始噪波非零、最终精确中心、两侧切线点积 `>0.995`、移动途经点前后同一 `t` 噪波完全一致、最终点减基础导向等于未经修改的全局噪波、Twist 全局角不重置。构建与静态验证通过，包内源码一致、前向函数引用 0，最终 MZP SHA256 为 `5260614123CE66185473721608C3568C4676E8336C1B1F747DAB790C84D27FFA`。 | 首次完整运行在所有 R-028 专项断言通过后，于旧的“自动同步失败后恢复”压力用例被既有 Twist 安全检查拒绝；该测试恢复位移已缩小，但后续独立 Max 测试实例未成功刷新结果文件，因此不能声称整套回归 PASS。最终截图场景视觉效果和完整核心／UI 回归仍待复验。 |
| 2026-08-12 | 途经点局部重布线与尖口修复 | R-011～R-013、R-022、R-025～R-029；对照 R-001～R-029 | 已定位“加面不在折角”和 V 形／扇形畸变的共同根因：旧逻辑把新增面计入整段采样，后续插入采样与基础采样重合时又只错开 `0.00001`，形成近零长度面；最短边曲率估算和曲率缩宽进一步放大尖口。现改为进入侧末端／离开侧起始的独立局部窗口，窗口内基础线环与新增线环统一均分；曲率半径改用三点外接圆；途经点平滑保持用户宽度，不再用缩窄截面掩盖急弯。用户指定的 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe` 通过完整核心回归，`artifacts/gui-smoke-result.txt` 于 23:45:33 写入 `PASS`，覆盖局部采样分布、最小间距、100 强度自动加面、途经点精确穿心、实际面／顶点／四边面／UV、噪波、Twist、原位更新及失败原子保护；最终 MZP UI 冒烟 `artifacts/ui-smoke-r029.txt` 于 23:47:12 写入 `PASS`，43 个控件通过。构建与静态包验证通过，最终包仅含入口与主脚本，SHA256 为 `6257E6FE38880C8BD66A95D42AFFE2043599FDA6CE24834420D11E5D28392E3E`。 | 尚未在用户截图中的手部／球体复杂场景人工观察最终视觉结果，因此 R-029 最后一项保持未勾选；物理鼠标拖放与 FBX 静态导出仍沿用既有待验状态。 |
| 2026-08-13 | 双途经点截面公平分角与 Twist 宽度保护 | R-011～R-013、R-022、R-024～R-030；对照 R-001～R-030 | 已定位单途经点正常、第二个途经点出现折角的根因：中间段首尾同时局部加密后，旧的跨点长弦／参数速度切线会被较长一侧拉偏，并把偏差传给平行运输和 Twist。内部截面切线现改为相邻实际边单位方向的公平角平分；平行运输与 Twist 共用该切线。自适应数据同时记录精确途经点站位，途经点平滑模式禁止 Twist 安全优化压窄这些站位；原平行运输基础带即使预检失败也不再静默切换到全局固定侧向，而是交由最终 Twist 安全检查决定提交或原子拒绝。用户指定的 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe` 最终核心回归写入 `artifacts/gui-smoke-result.txt = PASS`，新增断言覆盖强烈非均匀采样公平分角、双途经点相邻侧向连续、两个途经点 Twist 截面均保持 `5.0` 宽度、移动途经点后原节点原位同步，以及几何面／顶点／UV／命名／其他面片隔离。中文 UI 冒烟 `artifacts/ui-smoke-r030-final.txt = PASS`，43 个控件通过。构建与静态包验证通过，最终 MZP SHA256 为 `568CF4FCDB626C0A905F4197AE9FEDBFD6419DF6E2EF30CB976A6B30955D533B`。 | 用户本轮手部／球体双途经点截图场景仍需加载新包后人工视觉复验，因此 R-030 最后一项及 R-026～R-029 的相关视觉项保持未勾选；物理鼠标拖放、FBX 静态导出和远距离非相邻区段全局自相交仍沿用既有待验／范围外状态。 |
| 2026-08-13 | 多途经点恒宽与自动安全补面 | R-002、R-003、R-010～R-013、R-018、R-022、R-024～R-031；对照 R-001～R-031 | 已确认截图针口的直接原因是旧实现只保护途经点中心截面，Twist 安全优化仍会缩窄其相邻线环。现从首个途经点前的局部窗口到末个途经点后的局部窗口整体保护既有宽度倍率，真实起终点仍独立执行收尖／Twist 安全规则。两个以上途经点且平滑开启时，即使“平滑时允许额外加面”关闭，也以 4 倍局部密度预算自动添加恒宽安全线环；用户开关值保持不变，并另存 `XP_LightningWaypointSafetyAutoFaces`。100% 平滑控制柄改用相邻短段允许的完整受限导数长度，扩大圆角半径但继续精确穿心；接近掉头仍由夹角因子衰减。用户指定的 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe` 核心回归 `artifacts/gui-smoke-result.txt = PASS`，覆盖额外加面关闭仍自动补面、内部保护区每个截面宽度等于用户宽度乘真实端点收尖倍率、排序／移动途经点、节点原位更新、实际面数、连续四边面、UV、噪波、Twist、命名和其他面片隔离。最终 MZP 中文 UI 回归 `artifacts/ui-smoke-r031-final.txt = PASS`，43 个控件通过；包内源码一致性与静态验证通过，SHA256 为 `84639576B17F2C56BD616A4AB0A225C838B807CF9826E1E9D26FFF2E5D485F54`。 | 用户截图中的实际手部／球体场景仍需拖入本轮新包后人工视觉确认，因此 R-031 第二项以及 R-026～R-030 相关截图项保持未勾选；物理鼠标拖放、FBX 静态导出和远距离非相邻区段全局自相交仍沿用既有待验／范围外状态。 |
| 2026-08-13 | R-031 稳定备份与使用说明同步 | R-018、R-031；对照 R-001～R-031 | 用户确认当前表现可接受后，已将 R-031 多途经点恒宽版本建立为独立稳定回退点。`插件使用说明.md` 已纠正“实际面数始终等于基础面数”和“所有急弯都会安全缩宽”的旧表述，新增必需安全补面与主动额外加面的区别、途经点内部恒宽保护范围、基础／实际面数差异及对应常见问题。备份包含完整源码与测试、Max 2023 PASS 证据、当前 MZP、说明、需求和 SHA256 清单；MZP 与发布包字节一致。 | 本轮未修改 MaxScript 几何算法，沿用本轮此前已经取得的核心与 UI `PASS`；仍未执行 Windows 资源管理器物理拖放和 FBX 静态导出，远距离非相邻区段全局自相交仍不在当前范围。 |
| 2026-08-13 | GitHub 首次发布 | R-018；对照 R-001～R-031 | 已初始化本地 Git 仓库，敏感信息扫描未发现 GitHub 令牌、云密钥或私钥；仅命中测试所需的 `XP_UI_SMOKE_*` 运行标识。按安全默认在账号 `Xppppp-001` 下创建私有仓库 `XP-LightningPlane-Generator`，发布范围包括源码、测试、文档、当前 MZP、验证证据与 R-021／R-031 稳定备份，临时 `build/` 通过 `.gitignore` 排除。仓库地址：`https://github.com/Xppppp-001/XP-LightningPlane-Generator`。 | 仓库当前为私有；是否公开由用户后续决定。本轮不修改 MaxScript 几何算法，沿用 R-031 核心与 UI `PASS`。 |
| 2026-08-13 | GitHub V1.2.0 Release 下载页 | R-006、R-018、R-023；对照 R-001～R-031 | 按用户要求创建正式标签 `v1.2.0` 和 Release“XP闪电面片生成器 V1.2.0”，以当前 `main` 稳定提交为目标，并将 `dist/XP闪电面片生成器_V1.2.0.mzp` 以跨平台稳定附件名 `XP-LightningPlane-Generator_V1.2.0.mzp` 上传为可直接下载的安装包。发布页：`https://github.com/Xppppp-001/XP-LightningPlane-Generator/releases/tag/v1.2.0`；附件 SHA256 继续为 `84639576B17F2C56BD616A4AB0A225C838B807CF9826E1E9D26FFF2E5D485F54`。 | 仓库及 Release 当前为私有，只有获授权账号可下载；本轮不修改 MaxScript 算法，沿用 R-031 核心／UI `PASS`。 |
| 2026-08-13 | V1.3.0 整段朝向旋转 | R-032；对照 R-001～R-032 | 新增 `-180°～180°`“整段朝向旋转”，作为所有截面围绕局部切线的恒定角偏移，并与累计 Twist 独立相加；锁端只衰减 Twist，不衰减该角度。参数保存为 `XP_LightningWholeOrientationAngle`，旧面片默认 `0°`，支持选中载入、200ms 防抖原位更新和强制新建。宽度安全倍率仍只由原有 Twist 计算，整段朝向不暗中改宽；最终不安全几何原子拒绝。版本、中文 UI、构建脚本、测试、README 和使用说明统一为 V1.3.0。静态构建／包内一致性验证通过，MZP SHA256 为 `B795F247F6EFE48D8006C58171098B3C727583582FACC4545362191A29CCAAEC`。用户指定的 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe` 核心回归于 16:22:39 写入 `artifacts/gui-smoke-result.txt = PASS`；中文 UI 回归于 16:23:11 写入 `artifacts/ui-smoke-v130.txt = PASS`，44 个控件通过。 | 尚未用用户截图中的手部实际场景人工拖动该参数观察视觉朝向，也未执行 Windows 资源管理器物理拖放与 FBX 静态导出；远距离非相邻区段全局自相交仍不在当前范围。 |
| 2026-08-13 | 整段朝向与 Twist 完全解耦修复 | R-032；对照 R-001～R-032 | 根因确认：V1.3.0 首次实现虽在 UI 和保存层独立，但朝向非零时仍把未启用的 `wholeTwistAmount` 传入共用截面函数，导致面板保留的 Twist 数值混入结果。现以 `hasAxialTwist` 计算有效 Twist 贡献；未启用时强制为 `0°`，仅叠加恒定朝向角。缩宽优化仍只在真正启用 Twist 时运行。新增核心断言覆盖“Twist 关闭、分段 7／9、保留角度 120°、整段朝向 30°／45°”，逐截面验证恒定相对角、中心线与宽度不变；用户指定 Max 2023 核心回归于 16:38:41 写入 `PASS`，中文 UI／最终 MZP 回归写入 `artifacts/ui-smoke-v130-independent.txt = PASS`，44 控件通过。最终 MZP SHA256 为 `A3AED191F9E67D3EC8CDC23DBC3A9BA78300670B8E8062C56CA74FDECEE7157F`。 | 尚待用户在手部实际场景中视觉确认朝向；物理拖放、FBX 静态导出和范围外的远距离全局自相交状态不变。 |
| 2026-08-13 | V1.4.0 全路径恒宽与噪波相位／分轴强度 | R-033～R-034；对照 R-001～R-034 | 生成主链已彻底停止调用曲率／Twist 缩宽，宽度倍率只保留用户显式首尾收尖；基础面数不足时保持同一连续中心线并提高实际面数，使用既有曲率／Twist 风险权重集中重排线环，最高 `4000` 面，仍不安全则中文提示并原子保留旧模型。新增噪波相位、统一／分轴开关、统一值与世界 X/Y/Z 三轴值；分轴最小值为 `0`，支持单轴与全轴归零。六项参数贯穿 UI、200ms 原位更新、强制新建、用户属性保存／复制、选择载入和旧面片默认兼容。使用说明、README、MZP 元数据、构建与验证脚本统一升级为 V1.4.0。用户指定的 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe` 核心回归于 17:17:01 写入 `artifacts/gui-smoke-result.txt = PASS`，覆盖正负相位确定性、锁端、途经点穿心、统一／分轴等价、X 单轴、0/0/0、属性隔离、全路径恒宽、风险补面、连续四边面、弧长 UV、4000 面原子拒绝及旧版本迁移；最终 MZP 中文 UI 回归于 17:11:20 写入 `artifacts/ui-smoke-v140.txt = PASS`，50 个控件通过。静态包结构与包内源码一致性通过，最终 SHA256 为 `B4A40BB20893554D9662BA78F5CDA6EC0E89EC04775EA88443C85359CCDCEAD1`。 | 尚未在用户手部／球体原始复杂场景中人工拖动新相位与分轴参数并观察最终视觉；Windows 资源管理器物理拖放、FBX 静态导出及远距离非相邻区段全局自相交仍沿用既有待验／范围外状态。 |
| 2026-08-13 | 极端弯折自动重布线与归一化 UV | R-002～R-003、R-010、R-031、R-033～R-035；对照 R-001～R-035 | 新增双对角线质量评估，每个四边面同时检查两种三角化并选择面积有效、法线连续性更好的方向；固定 `0.70` 阈值不再作为生成主链的普通弯曲硬失败条件。安全报告区分真实退化／翻面与可细分风险，记录首个风险段和原因；补面预算改为按风险段数量及当前面数渐进增加，继续使用曲率／Twist 权重把线环集中到危险区，普通稳定非共面风险连续细分四轮后允许用最佳对角线提交，真实错误继续到 `4000` 面后原子拒绝。最终几何建立时通过 `polyop.setDiagonal` 写入所选对角线；UV 按最终中心线弧长重建，测试新增严格 U/V `0～1` 检查和用户截图参数的 10 随机种子压力用例。README、使用说明和需求已同步；`tools/build-package.ps1`、`tools/verify-package.ps1` 静态构建与包内一致性检查通过，包内仅含预期两文件、源码一致，最终 MZP SHA256 为 `15883CDA262D27A531FDAC5C6CDA67A1A5D34B94452B65CDF0BA31468409ADC9`。 | 当前执行通道启动用户指定的 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe` 时由 Windows 返回 `Access Denied`，因此新增 MaxScript 断言尚未取得新的实机 `PASS`，R-035 验收项保持未勾选；需要在可启动的 Max 2023 会话跑核心测试并在用户原场景视觉复验。 |
| 2026-08-13 | 抗扇形汇聚与贴图圆滑布线开发 | R-003、R-008、R-010～R-013、R-032～R-036；对照 R-001～R-036 | 根据用户截图确认贴图波浪的直接几何原因是恒宽带在急弯内侧形成扇形汇聚。新增连续抗收窄截面求解、中心／边界／截面角度与边界折返检查、弧长 UV、统一平滑组以及双对角线写入；修复 `polyop.setDiagonal` 误传全局顶点编号的问题，现按四边面内部角编号 `1–3`／`2–4` 设置。显式 Twist／整段朝向不再经过抗收窄侧倾，避免用户旋转被二次修改。指定 Max 2023 已多轮运行至新增几何断言和后续既有回归区段。 | 尚未取得本轮最终完整 `PASS`；并发测试曾触发第三方 OpenCL 插件提示，已只终止本轮测试实例并保留用户原 Max。后续仅在静态检查和打包完成后执行一次最终实机验证；用户原场景贴图效果仍待人工复验。 |
| 2026-08-13 | 普通参数误杀与无收益补面快速修复 | R-002～R-003、R-007、R-010、R-019、R-033～R-037；对照 R-001～R-037 | 根据用户截图中的精确报错 `0.98927` 定位到 R-036 首版 `0.99` 法线阈值把有效轻微非共面当作不可提交视觉风险，并且视觉风险不允许走细分收敛例外，导致 100 面机械增长到 4000 面仍失败。普通路径提交阈值调整为 `0.98`；安全报告新增法线风险计数与最低质量；抗收窄旋转预算由固定每截面 `0.20°` 改为整条归一化路径 `20°` 按采样间距分配；连续两轮风险数和最低质量无改善时提前停止，仅剩质量不低于 `0.97` 的轻微法线风险时使用最佳对角线提交，真实退化、翻面、零长度、折返和严重视觉风险仍拒绝。新增约 `0.989263` 合成四边面和用户截图普通参数回归；README、使用说明、验证脚本与 V1.4.0 MZP 已同步。静态括号／方括号、顶层 local、复合赋值、差异检查、UTF-8 BOM、前向函数引用、包内源码一致性和必要令牌验证通过。 | 为避免再次触发用户现有 Max 与第三方 OpenCL 插件的并发初始化冲突，本轮未启动第二个 Max；新增 MaxScript 断言和用户原场景仍待单实例 Max 2023 实机复验，R-037 验收项保持未勾选。 |
| 2026-08-13 | FFD 单次生成核心重构（静态阶段） | R-001～R-038，R-038 最高优先级覆盖旧补面／拒绝／缩宽路线 | 每条分支现按“规则直带 → 一次性面数预算 → 连续中心线 → 最小旋转平行运输 → 独立 Twist／整段朝向 → 可选端点收尖 → 一次 Editable Poly”生成。生成主链静态提取确认只含一次 `mesh name:`，且不含 `while`、`XP_LG_GetRibbonSafetyReport`、`XP_LG_BuildTextureStableSides`、`XP_LG_BuildTwistSafeWidthScales`、`XP_LG_BuildCurvatureWidthScales` 或 `XP_LG_BuildRouteCurvatureWidthScales` 调用；圆／方／花括号及 ASCII 引号平衡，`git diff --check` 通过。测试已加入 100 个随机种子、短／中／长路径、用户截图普通与高 Twist 参数、恒宽、连续四边面、严格 `0～1` UV、100／500／1000 面性能记录。README、中文使用说明、构建和包校验同步；最终静态包仅含 `mzp.run` 与主脚本，包内源码一致，SHA256 为 `3396CA3B197EE1A811678217AACE0B17DC7E937C06C98074E6F09F666A884B0B`。 | 当前用户原有 Max 2023（PID 95052）仍在运行。为避免再次触发并发 OpenCL 初始化与破坏用户场景，本轮没有启动第二实例；R-038 自动断言、性能实测、中文 UI 回归和用户原场景视觉确认仍待单实例验证，验收项保持未勾选。 |
| 2026-08-14 | 贴图三角锯齿拓扑修复（静态阶段） | R-039；回归映射 R-001～R-039 | 单列规则带扩展为长度 `N` × 宽度 `M` 的二维连续四边网格。`M` 在建模前以有限偶数候选确定：非近似严格共面时至少两列，使 `V=0.5` 成为真实纵向边；规划以相邻截面真实旋转为基线，将三角化额外法线误差压到 `0.25°` 内，真实 Twist 不会被误判或削弱。每条分支比较两种候选后只保存并应用一个全局对角线方向，禁止相邻面交替翻转。外轮廓顶点索引和用户宽度保持，内部列线性插值；UV 扩展为 U 弧长 × V 横向列的完整 `0～1` 网格。新增长度段、横向列、最终总面数、全局对角线及额外误差属性；原位替换会复制这些属性。核心测试已改为二维拓扑计数、横纵连续、中心边、UV 列、全局对角线、额外角误差、恒宽及 100／500／1000 长度段回归。静态验证通过：UTF-8 BOM、前向引用 0、主链仅一次 `mesh name:` 且无 `while`／旧视觉重建／缩宽调用，源码与测试圆方括号和引号平衡，`git diff --check` 通过；包仅含两个预期文件且包内源码一致，SHA256 为 `19CDFD93334309A86D224550A84A9915763FB1548AFDD8B44A3C01E0E78CF998`。README 与 `插件使用说明.md` 已同步。 | 用户指定路径的既有 Max 2023 进程 PID 95052 尚未关闭；为避免并发 OpenCL 初始化和破坏用户场景，未启动第二实例。R-038／R-039 核心自动断言、性能实测、中文 UI 回归以及用户原贴图场景视觉确认仍待完成，因此 R-039 验收项保持未勾选。 |
| 2026-08-14 | 用户控制加面与固定 Plane／FFD 变形（静态阶段） | R-040；回归映射 R-001～R-040 | 面板新增默认 `0`、范围 `0～1000` 的“额外加面”整数滑条；总长度面数只取 `平面面数 + 额外加面`，旧途经点自动加面控件已移除。普通路径改为按规则 Plane 的等距逻辑参数采样，途经点路径按段长分配固定总线环并在每段等距采样；曲率、噪波、Twist、途经点平滑和非共面程度均不再重新分配或增加线环。主链固定宽度一列，只创建一次 Editable Poly；基础 `10`／额外 `0` 与基础 `10`／额外 `20` 的核心断言分别要求 `10` 面／`22` 顶点和 `30` 面／`62` 顶点。选择载入、200ms 原位更新、强制新建、旧面片默认 `0`、弧长 `0～1` UV、README 与中文使用说明均已同步。静态构建和包验证通过：UTF-8 BOM、前向函数引用 `0`、主链无旧自动密度／横向列调用、普通及途经点映射无曲率／Twist 权重重排、包内源码一致且仅含两个预期文件；MZP SHA256 为 `BC0649D7C237F843C7F5821B93B53405613982153B28F20C25A4BA03726DE9B2`。 | 当前用户正在使用唯一的 `C:\Program Files\Autodesk\3ds Max 2023\3dsmax.exe` 实例（PID 95052）。为避免第二实例再次触发 OpenCL 初始化错误或重置用户场景，本轮未执行核心／UI 实机回归；R-040 验收项保持未勾选，需在可用的隔离单实例 Max 2023 中执行 `tests/smoke.ms`、UI 回归并由用户在原贴图场景确认视觉结果。 |
| 2026-08-14 | V1.4.0／R-040 当前快照备份与 GitHub 同步 | R-006、R-018、R-021、R-040；对照 R-001～R-040 | 已建立 `backups/2026-08-14_V140_R040_fixed_plane_ffd_snapshot`，包含当前完整源码 ZIP、R-040 MZP、需求、中文使用说明和 SHA256 清单。当前 MZP 与 `dist` 字节一致，SHA256 为 `BC0649D7C237F843C7F5821B93B53405613982153B28F20C25A4BA03726DE9B2`；发布包复核通过 UTF-8 BOM、内部函数前向引用 `0`、包内源码一致和仅含两个预期文件。提交 `80f0c3d` 已推送到 `agent/v140-r040-backup`，并创建草稿 PR `#1`：`https://github.com/Xppppp-001/XP-LightningPlane-Generator/pull/1`。 | 这是静态阶段当前快照，不宣称 R-040 已完成 Max 2023 实机回归；PR 当前为草稿且尚未合并到 `main`。 |

## 6. 需求变更记录

| 日期 | 变更内容 | 状态 |
| --- | --- | --- |
| 2026-08-07 | 建立 V1 基线：一个起点到多个终点、写实噪波路径、自动 UV、端点锁定、起点轴心、中文界面和 3ds Max 2023 拖放使用 | 已确认 |
| 2026-08-07 | 新增“平面面数”控制；每条分支最低支持一个四边形，并可生成指定数量的连续四边面 | 已确认 |
| 2026-08-07 | V1.1.0 新增参数实时更新：数值、锁定选项和控制点变化后自动同步模型，不再依赖手动更新 | 已实现并实机验证 |
| 2026-08-07 | V1.1.0 最初提出“整段扭曲”；当时记录为大尺度弯折的解释已于 2026-08-11 作废，以后续真正的轴向 Twist 定义为准 | 已更正 |
| 2026-08-11 | 用户纠正“整段扭曲”语义：必须实现沿中心线切线轴旋转面片截面的真正 Twist；此前将其实现为额外中心线 Noise／大尺度弯折的方案作废 | 已实现并实机验证 |
| 2026-08-12 | 新增“面片两端平滑收尖”选项：每条分支两端平滑缩窄；为避免退化面，端点保留正常宽度的 1%，并保持中心线、四边面拓扑、轴心和 UV 不变 | 已实现并实机验证 |
| 2026-08-12 | 用户要求优化 Twist 面片异常：新增局部非共面／翻折安全检查；同时将多终点输出从单个合并模型改为“每个终点一个独立新对象”，整组更新仍保持原子替换 | 已实现并实机验证 |
| 2026-08-12 | 用户要求从算法层避免急弯挤线并动态布线：保持最终面数精确不变，按曲率／Twist 重新分配线环，并在曲率半径不足时平滑收窄局部宽度，生成后安全检查仅作为最后保护 | 已实现并实机验证 |
| 2026-08-12 | 用户要求将 Twist 改为类似 FFD 的效果：先在拉直逻辑空间累计旋转，再通过最小旋转框架映射回噪波中心线，避免路径弯曲放大 Twist 角度 | 已实现并实机验证 |
| 2026-08-12 | 用户进一步明确 Twist 分段语义：分段数 `K` 将面片全长等分为 `K+1` 份，前 `K` 份各按输入角度逐段累计旋转，最后一份负责保持或受终点锁定平滑回正；原先“分段仅细微影响整条总角度插值”的解释作废 | 已实现并实机验证 |
| 2026-08-12 | 新增分支对象命名规则：首次生成依次使用 `LightningPlane001`、`LightningPlane002`……；同一终点因参数变化被原子重建时继承旧对象现有名称，新增或删除终点不得重命名其他分支 | 已实现并实机验证 |
| 2026-08-12 | 修复强制重新生成覆盖旧面片：按钮必须保留当前组并新建一套 `LightningPlane###`；新组成为后续实时更新目标，历史组不再被修改或删除 | 已实现并实机验证 |
| 2026-08-12 | 新增一键 Unity 轴向适配：仅修改当前活动组的局部轴向为 Y-Up／Z-Forward 对应关系，保持世界几何、UV、名称和起点轴心不变，并支持幂等与 Undo | 已实现并实机验证 |
| 2026-08-12 | 用户确认 Unity 导出对象的 Rotation 必须为 `0/0/0`：Unity 轴向转换改为烘焙顶点，使用 `(x,y,z)→(x,z,-y)` 的无镜像映射，节点旋转清零、缩放归一；旧版 X=90° 对象需支持自动迁移 | 已实现并实机验证 |
| 2026-08-12 | 新增独立中文 `插件使用说明.md`：完整对应当前面板功能；以后功能、控件、默认值、范围、工作流或限制发生变化时，必须在同一轮开发中同步更新并复核 | 已确认，待编写与核对 |
| 2026-08-12 | 用户纠正 Unity 轴向归零语义：Rotation 必须为 `0/0/0`，但适配前后 Max 中模型的实际世界位置、朝向和轮廓不得变化；改用内部轴向旋转与局部顶点逆补偿，旧的“直接烘焙并让视口模型转向”方式作废 | 已修复，待 3ds Max 2023 实机复验 |
| 2026-08-12 | 修复 Unity 轴向按钮运行时报错：`rotationPart` 是 Matrix3 实例属性而不是全局函数，必须通过 `unityAxisRotation.rotationPart` 读取后再取逆四元数 | 已修复，待用户实机复验 |
| 2026-08-12 | 用户最终简化轴向需求：只把生成模型的本地轴心改为 Y 朝上，本地 X 不变，模型世界空间位置与朝向完全不动；使用冻结旋转基准使当前可编辑旋转参数为 `0/0/0`，Object Offset 补偿方案作废 | 已实现，待 3ds Max 2023 实机复验 |
| 2026-08-12 | 用户纠正生成／编辑逻辑：普通参数调整只修改场景中当前选中的单个插件面片，保持同一节点、名称和层级，不另外生成；切换选中面片后载入并编辑该面片自己的参数；只有点击“强制重新生成”才创建新的 `LightningPlane###` 对象 | 已实现，待 3ds Max 2023 实机复验 |
| 2026-08-12 | 用户撤销 Unity／Y-Up 轴向修改需求：移除面板轴向按钮及全部轴向适配、冻结旋转和继承逻辑；新生成／更新面片不得再由插件安装 `Rotation List`，避免 FBX 的 `Unsupported controller` 警告；旧场景节点不在加载时自动修改 | 已实现并完成静态验证，待 3ds Max 2023／FBX 实机复验 |
| 2026-08-12 | 用户要求恢复 `插件使用说明.md` 的编写和同步维护；本轮需按当前面板与最新生成／编辑逻辑重写说明，并移除全部已撤销的 Unity／Y-Up 轴向内容 | 已完成并静态核对 |
| 2026-08-12 | 插件正式名称改为“XP闪电面片生成器”；同步修改面板、提示标题、MZP 元数据、安装包文件名和文档，同时保留 `LightningPlane###` 与 `XP_LG_*` 内部兼容标识 | 已实现并完成静态验证，待 Max 2023 界面与旧面片兼容性复验 |
| 2026-08-12 | 修复他人电脑首次加载 MZP 后点击“拾取起点”在主脚本第 84 行发生 `undefined` 函数调用：拾取过滤器不得前向调用尚未定义的内部函数，也不得依赖开发机旧会话的全局状态 | 已修复并完成静态验证，待对方干净 Max 2023 实机复验 |
| 2026-08-12 | 用户要求先备份当前稳定状态，再新增“途经点”功能；途经点用于让闪电在起点与终点之间经过指定控制对象 | 已备份；功能细节已确认，开发中 |
| 2026-08-12 | 用户确认途经点按当前所选 `LightningPlane` 层级独立保存，不影响其他面片；支持按列表顺序经过及上移、下移、移除、清空；路径精确经过对象中心，途经点只改变整体基础走向，不改变噪波、Twist 或其他任何现有参数 | 已实现并通过 Max 2023 自动验证 |
| 2026-08-12 | 用户要求将插件当前版本号由 `V1.1.0` 统一升级为 `V1.2.0`，同步当前交付包、面板、脚本、测试和文档，并保持旧面片兼容 | 已实现并通过 Max 2023 自动验证 |
| 2026-08-12 | 用户要求修复移动途经点后重新选择所属面片仍不更新的问题：保存上次成功控制点坐标快照，选择时仅在坐标发生变化时防抖原位同步，失败保留旧模型且不得递归重试 | 已实现并完成静态验证，待 Max 2023 实机复验 |
| 2026-08-12 | 用户要求改善途经点处的生硬折角：新增可关闭的途经点平滑过渡与 `0～100%` 强度，使用受段长和夹角限制的共享切线曲线，仍精确经过中心并保持固定面数、动态布线、UV 及其他参数职责 | 已实现并完成静态验证，待 Max 2023 实机复验 |
| 2026-08-12 | 用户反馈强度 `100` 时离散折角仍明显：将过渡强度改为滑条，并新增默认关闭的“平滑时允许自动加面”；开启后按途经点转角和强度在相邻段局部补充四边面，区分基础面数与实际面数 | 已实现并完成静态验证，待 Max 2023 实机复验 |
| 2026-08-12 | 用户实机确认自动加面后途经点折角仍存在且出现扇形近似破面；修复目标改为最终噪波中心线 C1 连续、连续全局噪波场及跨相邻面的边界／截面折叠防护，自动加面只负责采样连续曲线 | 已实现并通过 Max 2023 核心回归，待用户复杂场景视觉复验 |
| 2026-08-12 | 用户纠正上一轮职责边界：途经点只允许改变整体基础走向，不得影响路径噪波或轴向 Twist。取消途经点局部噪波窗与局部噪波轴；改用基础导向控制点预补偿全局噪波，从而既精确经过中心，又保持整条分支的噪波场和 Twist 逻辑角不变 | 已实现并通过 Max 2023 专项断言，待完整回归与用户场景视觉复验 |
| 2026-08-12 | 用户指出自动加面没有集中在折角且途经点产生 V 形尖口。修复为：新增面使用途经点两侧独立局部过渡窗口，窗口内基础与新增线环统一重排以消除近零长度面；曲率宽度使用非均匀采样稳定的三点外接圆半径；途经点平滑保持用户宽度，无法安全表达时原子拒绝而不是提交尖口 | 已实现并通过 Max 2023 核心／UI 自动回归，待用户复杂场景视觉复验 |
| 2026-08-13 | 用户确认单途经点正常、添加第二个途经点后出现折角和局部扭曲。修复目标：多途经点首尾加密段使用相邻边单位方向的公平角平分切线；平行运输与 Twist 共享该切线；途经点平滑模式不允许 Twist 安全缩宽生成尖口 | 已实现并通过 Max 2023 核心／UI 自动回归，待用户截图场景视觉复验 |
| 2026-08-13 | 用户进一步确认第二个途经点仍出现局部缩小：途经点只允许改变方向，不得改变面片宽度或形状；为保持恒宽和避免畸变，插件可在多途经点转向区域自动增加安全线环，即使主动自动加面选项未勾选也不得以缩宽代替 | 已实现并通过 Max 2023 核心／UI 自动回归，待用户截图场景视觉复验 |
| 2026-08-13 | 用户确认当前多途经点恒宽版本表现可接受，要求将 R-031 当前源码、MZP、需求与说明建立为新的稳定回退点，并同步完善 `插件使用说明.md` 中恒宽保护、安全补面、基础／实际面数和急弯提示 | 已完成并核对 |
| 2026-08-13 | 用户要求把当前 R-031 稳定版上传到 GitHub；首次仓库按安全默认创建为私有仓库，上传源码、测试、文档、当前 MZP、验证证据与稳定备份，并排除临时构建目录 | 已创建私有仓库并完成发布记录 |
| 2026-08-13 | 用户要求为当前 V1.2.0 稳定版创建 GitHub Release 下载页面，并附加可直接拖入 3ds Max 2023 的 MZP 安装包 | 已创建并验证下载附件 |
| 2026-08-13 | 用户要求新增独立“整段朝向旋转”，用于在不改变中心线、途经点、噪波、Twist、宽度、面数、UV、轴心、命名和其他面片的前提下调整整条带状面片朝向；当前版本升级为 V1.3.0 | 已实现并通过 Max 2023 核心与中文 UI 回归 |
| 2026-08-13 | 用户反馈整段朝向旋转仍错误依托 Twist：Twist 未启用时不得应用面板中保留的分段／角度值，整段朝向必须是独立恒定旋转，且不得触发任何缩宽或影响其他参数 | 已修复并通过 Max 2023 核心与中文 UI 回归 |
| 2026-08-13 | 用户要求彻底取消所有内部缩宽：调整噪波大小、强度、Tiling、Twist、途经点或整段朝向产生弯折时，允许在真实风险位置自动加面，但不得改变用户宽度；只有主动两端收尖可影响真实端点 | 已实现并通过 Max 2023 核心／UI 回归 |
| 2026-08-13 | 用户要求为路径噪波新增沿路径推进图样的相位位移；轴向强度采用世界 X/Y/Z，提供统一控制与分开控制开关，各轴最小值为 0 并支持单轴噪波 | 已实现并通过 Max 2023 核心／UI 回归 |
| 2026-08-13 | 用户确认极端情况允许插件自动优化局部布线，不必拘泥基础面数或固定对角线；结果必须保持中心路径与用户宽度，避免高概率生成失败，并将最终每个独立分支的 UV 重新归一化到 `0～1` | 已实现并通过静态构建／包校验，待 Max 2023 实机回归 |
| 2026-08-13 | 用户反馈已生成模型仍存在明显折边，贴图映射后主线呈波浪和锯齿；新增视觉圆滑质量底线，按中心线／边界／截面转角与四边面非共面程度自动在真实风险区加面，最终 UV 继续严格归一化到 `0～1` | 已实现主体算法，待最终完整实机回归与原场景视觉确认 |
| 2026-08-13 | 用户反馈普通单起终点、100 面、宽度 3、噪波强度 10、噪波大小 1、Tiling 1、Twist 关闭也因最佳法线点积 `0.98927` 补到 4000 面后失败；要求快速恢复普通参数可靠生成。视觉阈值改为 `0.98`，截面旋转按路径长度分配，并在补面无收益时提前收敛，轻微有效非共面使用最佳对角线提交 | 已实现并完成静态构建／包校验，待单实例 Max 2023 实机复验 |
| 2026-08-13 | 用户要求重新梳理此前全部畸变、破面、高失败率和卡顿问题，并确认按 FFD 思路严谨推进：规则直带只做一次连续映射，生成前一次解析噪波／Twist／途经点平滑密度，生成后不再循环补面、视觉阈值拒绝或内部缩宽；历史“最终面数必须等于基础面数”和未勾选途经点仍暗中安全补面的规则由 R-038 统一覆盖 | 主链、测试、文档和 MZP 静态校验已完成，待单实例 Max 2023 核心／UI 回归与用户原场景视觉确认 |
| 2026-08-14 | 用户反馈 FFD 单次生成版本的贴图中仍存在明显三角形锯齿。新增 R-039：将仅有一列的非共面四边带扩展为按需横向细分的二维规则网格，并把逐面独立对角线改为整条分支统一对角线；保持中心线、外轮廓宽度、Twist／朝向、途经点和 `0～1` UV 语义 | 已实现并完成静态构建／包校验，待单实例 Max 2023 回归与用户原贴图场景视觉确认 |
| 2026-08-14 | 用户否定隐藏自动加面与横向网格方案：基础 10 面被扩大到 528 面不可接受。新增 R-040，以“平面面数 + 用户额外加面滑条”作为唯一面数来源；先建立规则直线 Plane，再用类似 FFD 的一次连续映射施加全部参数。FFD 是内部变形方式，不是滑条；噪波、Twist、途经点和非共面程度均不得暗中加面或改变宽度 | 已完成静态实现与打包，待单实例 Max 2023 核心／UI 回归及用户场景视觉确认 |
| 2026-08-14 | 用户要求备份当前 V1.4.0／R-040 固定 Plane 与 FFD 变形状态并上传 GitHub；备份必须包含当前源码、测试、文档、需求、MZP 与 SHA256 清单，并明确当前仅完成静态校验、Max 2023 实机回归仍待执行 | 已完成本地备份，已推送 GitHub 分支并创建草稿 PR #1 |
